\pdfminorversion=7
\pdfobjcompresslevel=0
\documentclass{article}
\usepackage{iclr2026_conference,times}

\input{math_commands.tex}

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{mathtools}
\usepackage{booktabs}
\usepackage{array}
\usepackage{graphicx}
\usepackage{placeins}
\usepackage{float}
\usepackage{hyperref}
\hypersetup{hidelinks}
\usepackage{url}

\title{MatrixPolicy for Role-Aware Optimization of Rational Transformer Nonlinearities}

\author{Anonymous Authors}

\newcommand{\clip}{\operatorname{clip}}
\newcommand{\rms}{\operatorname{rms}}
\newcommand{\geom}{\operatorname{geomean}}
\newcommand{\relu}{\operatorname{ReLU}}
\newcommand{\smooth}{\operatorname{smoothstep}}
\newcommand{\RLB}{\mathrm{RLB}}
\newcommand{\diag}{\operatorname{diag}}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}

\setlength{\textfloatsep}{16pt plus 2pt minus 2pt}
\setlength{\floatsep}{12pt plus 2pt minus 2pt}
\setlength{\intextsep}{12pt plus 2pt minus 2pt}
\setlength{\abovecaptionskip}{5pt}
\setlength{\belowcaptionskip}{9pt}
\raggedbottom

\begin{document}

\maketitle

\begin{abstract}
A useful LLM pretraining recipe moves the loss-time frontier by reaching the
same validation loss with fewer tokens or less wall-clock time.  We target this
objective through the Transformer nonlinear sublayer, treating it as an
optimizer-visible interface.  MatrixPolicy with Rational Latent Basis (RLB)
replaces a fixed scalar response with grouped rational responses that expose
local gain, coefficient-activity, and matrix-balance statistics to a role-aware
update.  MatrixPolicy restricts the signal-conditioned update to the sublayer
input and output matrices; rational coefficients and the rest of the Transformer
remain on AdamW.  Across DCLM, FineWeb-Edu, FineWeb, Dolma-sample, and C4 with a
fixed 12-layer, width-768 decoder, MatrixPolicy reaches matched validation-loss
targets with fewer tokens and lower measured arrival time than SiLU controls
under the same protocol.  It saves 19.5--22.8\% target-arrival tokens versus
SiLU with AdamW and 29.2--34.0\% versus SiLU with Muon at 100M tokens, and
22.3--26.4\% and 6.7--9.8\%, respectively, at 300M tokens.  [todo: Insert the
larger-scale training result here.]
\end{abstract}

\section{Introduction}
\label{sec:introduction}

Recent LLM optimizer papers frame practical efficiency through loss-time and
compute-time frontiers, asking which training recipe reaches a target loss with
fewer tokens, FLOPs, or wall-clock time
\citep{shah2025practicalefficiency,liu2025muon,semenov2025benchmarking}.  This
criterion also exposes an activation-design question.  In a modern decoder
block, the nonlinear sublayer is a learned matrix--nonlinearity--matrix map.  The
activation sets local gain and curvature, while the adjacent matrices choose and
recombine the coordinates on which that response acts.  Standard training
pipelines leave this local structure implicit to the optimizer.  Activation
coefficients and adjacent matrices are updated as ordinary parameters, leaving
gain, coefficient-activity, and role information inside the sublayer unused by
the matrix update.

We make this interface concrete with Rational Latent Basis (RLB) and
MatrixPolicy.  RLB maps residual states into grouped latent coordinates,
normalizes each group by its RMS radius, and applies a trainable real-pole-free
P5/Q4 rational response.  The resulting response-gain, coefficient-activity,
matrix-pressure, and pair-balance signals are detached from the forward graph and
passed to MatrixPolicy.  MatrixPolicy restricts their influence to the RLB input
and output matrices; attention, embeddings, normalization parameters, rational
coefficients, and ordinary Transformer tensors remain on AdamW.  This local
division exposes the quantities created by the nonlinear sublayer while
preserving AdamW for the rest of the model.

We evaluate target arrival by measuring the tokens and wall-clock time required
to reach a fixed validation-loss target under the same model, corpus, and
protocol.
In the completed study, the same 12-layer, width-768 causal Transformer is
trained on DCLM, FineWeb-Edu, FineWeb, Dolma-sample, and C4.  The experiments
include RLB-only controls and matched SiLU with AdamW and SiLU with Muon
baselines under the same model, data, and measurement protocol.  MatrixPolicy
reaches shared validation-loss targets earlier at both 100M and 300M token
budgets.  [todo: Insert the larger-scale training result here.]  The contribution
is an activation design that exposes optimizer-visible matrix roles, a localized
role-aware optimizer for the exposed matrix pair, and matched evidence that this
interface reduces target-arrival tokens and wall-clock time across five
pretraining corpora.

\section{Related Work}
\label{sec:related}

Prior work on Transformer nonlinear sublayers has developed the projection
topology and scalar response along largely separate lines.  The topology line
begins with multiplicative GLU gates and reaches Transformer sublayers through
SwiGLU \citep{dauphin2017language,shazeer2020glu}.  Related gated forms are used
from LLaMA through Llama~3 and in recent DeepSeek-V3 and Qwen3 models
\citep{touvron2023llama,grattafiori2024llama3,deepseekai2024deepseekv3,
yang2025qwen3}.  These architectures learn the surrounding projections while
retaining a response family selected before pretraining.

The response line instead makes the scalar map trainable.  Rational networks
provide efficient smooth-function approximation and recent expressivity
separations from fixed and gated Transformer responses
\citep{boulle2020rational,tang2026rationalexpressivity}.  Pad\'e Activation Units
established end-to-end learning of rational coefficients, and later work studied
learnable rational activations in Transformer pretraining and fine-tuning
\citep{molina2020pade,fang2023transformers}.  Within a
matrix--response--matrix sublayer, the realized response enters the
output-projection gradient and its derivative enters the input-projection
gradient, as derived in Appendix~\ref{app:matrixpolicy-proxies}.  Coefficient
learning makes both factors evolve and supplies coefficient-gradient activity as
an additional local signal.  The rational-activation formulations above retain a
base optimizer for the coefficients and adjacent matrices, leaving a
response-summary interface outside their scope.

Optimizer work differs primarily in the parameter structure used to transform
those gradients.  AdamW retains coordinatewise moment adaptation with decoupled
weight decay \citep{loshchilov2019decoupled}.  Sophia, Adam-mini, and GaLore add
diagonal curvature, block-shared rates, or low-rank gradient subspaces
\citep{liu2024sophia,zhang2025adammini,zhao2024galore}.  SOAP and Muon extend
adaptation to matrix geometry through an evolving eigenbasis or orthogonalized
momentum \citep{vyas2025soap,liu2025muon}.  Across these methods, conditioning is
derived from gradient history, curvature, and parameter geometry.  The
nonlinear response that produced the gradients and the input/output roles of its
adjacent matrices remain implicit.

MuonClip provides a directly relevant example of module-conditioned
optimization.  Its maximum attention logit triggers post-update rescaling of the
query and key matrices that produced it \citep{kimiteam2025kimik2}.  MatrixPolicy
applies this module-conditioned viewpoint to the nonlinear sublayer, where
detached response statistics condition the two projections surrounding a learned
activation.  This links learnable activation and matrix-aware optimizer work
through a module-specific control interface.

\section{Method}
\label{sec:method}

RLB and MatrixPolicy instantiate this response-conditioned matrix-pair
interface.  RLB groups trainable rational responses behind an RMS normalization,
which gives the surrounding matrix pair a reciprocal scale coordinate along
which MatrixPolicy takes bounded steps.  The learned responses expose
role-specific response statistics and coefficient activity.  MatrixPolicy
consumes detached summaries of this local state to allocate
groupwise gradients, shape the matrix update, and balance the pair scale.
Rational coefficients and ordinary Transformer parameters remain on the AdamW
path in the evaluated configuration.

The bounded gates and schedule transitions below use
\begin{align}
    \clip(x,a,b)&=\min\{\max\{x,a\},b\}, \\
    \smooth(a,b,x)&=3\omega^2-2\omega^3,
    \qquad
    \omega=\clip\left(\frac{x-a}{b-a},0,1\right).
\end{align}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/matrixpolicy_overview.pdf}
\caption{Activation--optimizer interface for RLB.  In layer
$l$, $A_l$ maps the sublayer input into normalized rational groups and $B_l$
recombines the rational features
$h_l=\operatorname{concat}_{g=1}^{G}h_{l,g}$.  Forward RLB statistics, gradient
pressure/activity EMAs, and pair-balance measurements cross a stop-gradient
boundary as policy inputs.  MatrixPolicy reads these summaries and writes
$\Delta A_l$ and $\Delta B_l$; rational coefficients are AdamW-trained and
policy-observed.}
\label{fig:matrixpolicy-overview}
\end{figure}

\subsection{Rational Latent Basis}
\label{sec:method-rlb}

For layer $l\in\{1,\ldots,L\}$, let $x_l\in\mathbb{R}^{d}$ be the input to the
nonlinear sublayer.  RLB uses an input matrix $A_l\in\mathbb{R}^{H\times d}$ and
an output matrix $B_l\in\mathbb{R}^{d\times H}$ with latent width $H=Gm$.  Let
$A_{l,g}\in\mathbb{R}^{m\times d}$ denote the row block of $A_l$, and let
$B_{l,g}\in\mathbb{R}^{d\times m}$ denote the matching column block of $B_l$.
In the implementation, $A_l$ and $B_l$ are the bias-free
\texttt{in\_proj.weight} and \texttt{out\_proj.weight} of the nonlinear
sublayer.  The evaluated runs use an RMS floor
$\epsilon_{\rm rms}=10^{-6}$.
Each group has a trainable P5/Q4 rational response
\begin{align}
    P_{l,g}(u)&=\sum_{i=0}^{5}a_{l,g,i}u^i,
    \label{eq:rlb-p}\\
    Q_{l,g}(u)&=
    1+|b_{l,g,1}|\,|u|+|b_{l,g,2}|\,u^2
     +|b_{l,g,3}|\,|u|^3+|b_{l,g,4}|\,u^4,
    \label{eq:rlb-q}\\
    R_{l,g}(u)&=P_{l,g}(u)/Q_{l,g}(u).
    \label{eq:rlb-response}
\end{align}
Define the preactivation, RMS radius, normalized coordinate, and grouped
rational feature by
\begin{align}
    z_{l,g}(x_l)
    &=
    A_{l,g}x_l,
    \qquad
    \rho_{l,g}(x_l)
    =
    \sqrt{m^{-1}\lVert z_{l,g}(x_l)\rVert_2^2+\epsilon_{\rm rms}},
    \label{eq:rlb-coordinate}\\
    u_{l,g}(x_l)
    &=
    \frac{z_{l,g}(x_l)}{\rho_{l,g}(x_l)},
    \qquad
    h_{l,g}(x_l)
    =
    \rho_{l,g}(x_l)R_{l,g}(u_{l,g}(x_l)),
    \qquad
    h_l=\operatorname{concat}_{g=1}^{G}h_{l,g}.
    \label{eq:rlb-feature}
\end{align}
The RLB forward map is the single block expression
\begin{equation}
    y_l
    =
    B_lh_l
    =
    \sum_{g=1}^{G}
    B_{l,g}
    \rho_{l,g}(x_l)
    R_{l,g}\!\left(
    \frac{A_{l,g}x_l}{\rho_{l,g}(x_l)}
    \right).
    \label{eq:rlb-forward}
\end{equation}
The scalar functions $P_{l,g}$, $Q_{l,g}$, and $R_{l,g}$ are applied
coordinatewise when their input is a vector.  Since $Q_{l,g}(u)\ge1$ for all
real $u$, the implemented response is pole-free on $\mathbb R$.  The response is piecewise
rational because of the absolute-value terms; derivative statistics use the
autodiff convention $\sign(0)=0$ and equations involving $R'_{l,g}$ are
interpreted almost everywhere.  Numerator and denominator coefficients are
initialized by a SiLU fit on $[-5,5]$ and are then trained by AdamW.  The evaluated
model uses these groupwise P5/Q4 rational responses.

The forward map in \eqref{eq:rlb-forward} separates the normalized coordinate
at which the rational response is evaluated from the group radius returned to
the residual stream.  This radius-factor form exposes the positive homogeneity
behind the matrix-pair coordinate formalized in
Appendix~\ref{app:rlb-calculus}.  The floor
$\epsilon_{\rm rms}>0$ is an additive squared-RMS stabilizer.  For fixed curves
$R_l=\{R_{l,g}\}_{g=1}^{G}$, write this block map as $f_l(x;A_l,B_l,R_l)$.
For any fixed input $x$ such that
$A_{l,g}x\ne0$ for every group, if $\epsilon_{\rm rms}=0$, then for any
$\lambda\in\mathbb{R}_{>0}^{G}$ and
$D_l(\lambda)=\diag(\lambda_1I_m,\ldots,\lambda_GI_m)$,
\begin{align}
    f_l(x;A_l,B_l,R_l)
    =
    f_l(x;D_l(\lambda)A_l,B_lD_l(\lambda)^{-1},R_l).
    \label{eq:positive-rescale}
\end{align}
This positive matrix-pair rescaling is exact in the unfloored map.  The RMS
floor perturbs the orbit, and MatrixPolicy follows the resulting approximate
coordinate with bounded steps.

\subsection{MatrixPolicy}
\label{sec:method-update}

MatrixPolicy is the optimizer-side policy attached to the RLB matrix pair
$(A_l,B_l)$.  We use the name for the matrix-specific controls in the evaluated
optimizer configuration.  Other Transformer tensors follow their assigned
backbone optimizer, and the rational coefficients contribute activity signals
while retaining their AdamW no-decay updates in the reported runs.  The policy
reads detached summaries.  These summaries are produced by the RLB forward
hook, by post-global-clipping gradients observed before MatrixPolicy rescales
them, and by fixed-grid probes of the rational response.  They feed, in order,
group-gradient gating, a role/depth matrix step, and reciprocal pair rescaling.

This ordering matters.  Let $t$ be the one-based optimizer step after the
wrapper increments its counter, $T$ the planned number of steps, and
$\xi_t=t/T$.  The surrounding optimizer wrapper first constructs detached
gradient memories from the current step; the inner matrix optimizer uses those
memories to rescale matrix gradients and apply AdamW/Muon updates; the wrapper
then measures the updated matrix pair and applies the reciprocal rescale.  No
summary tensor enters the forward graph.  For notation, set
$M_{l,\mathrm{in}}=A_l$ and $M_{l,\mathrm{out}}=B_l$, with
$\sigma\in\{\mathrm{in},\mathrm{out}\}$ denoting the two roles.  For any tensor
$V$, define
\begin{align}
    \rms(V)=\sqrt{\operatorname{mean}(V^2)},
    \qquad
    \rms_{\epsilon}(V)=\sqrt{\operatorname{mean}(V^2)+\epsilon}.
    \label{eq:rms-definition}
\end{align}
The evaluated implementation uses $\epsilon_{\rm live}=10^{-6}$ for cached
forward gains and $\epsilon_p=\epsilon_{\rm probe}=10^{-8}$ for pressure,
matrix-ratio, and fixed-grid probe floors.

The first family of summaries is the on-policy pressure and activity memory.
Here ``on-policy'' means that the wrapper records statistics from the current
training trajectory.  All gradients in this paragraph are optimizer-visible loss
gradients after global clipping and before MatrixPolicy group-gradient scaling.
Let $\Theta^R_{l,g}$ collect the trainable rational-response coefficient blocks
in group $(l,g)$; for the reported P5/Q4 response these are the numerator and
denominator coefficient tensors.  The count $|\Theta^R_{l,g}|$ is a block count,
and each $\operatorname{mean}$ is over scalar entries in one block.  The
instantaneous detached measurements are
\begin{align}
    p^{\rm in}_{l,g}
    &=
    \frac{\rms_{\epsilon_p}(\nabla A_{l,g})}
    {\rms_{\epsilon_p}(A_{l,g})},
    &
    p^{\rm out}_{l,g}
    &=
    \frac{\rms_{\epsilon_p}(\nabla B_{l,g})}
    {\rms_{\epsilon_p}(B_{l,g})},
    \label{eq:matrix-pressure}\\
    p^{R}_{l,g}
    &=
    \left[
    \frac{1}{|\Theta^R_{l,g}|}
    \sum_{\theta\in\Theta^R_{l,g}}
    \operatorname{mean}((\nabla\theta)^2)
    +\epsilon_p
    \right]^{1/2}.
    \label{eq:rational-activity-raw}
\end{align}
The matrix quantities are relative RMS-gradient proxies, while $p^R_{l,g}$ is
raw coefficient-gradient energy.  The wrapper stores detached EMA memories
$q^{\rm in}$, $q^{\rm out}$, and $q^R$,
\begin{align}
    q^{r}_{l,g,t}
    =
    0.95\,q^{r}_{l,g,t-1}+0.05\,p^{r}_{l,g,t},
    \qquad
    r\in\{\mathrm{in},\mathrm{out},R\},
    \label{eq:pressure-ema}
\end{align}
initialized from the first observed values.  We reserve $q$ in this subsection
for these detached policy memories; the rational denominator remains denoted by
$Q_{l,g}$.  The two contrasts used by all three controls are
\begin{align}
    \pi_{l,g}
    =
    \log q^{\rm in}_{l,g}-\log q^{\rm out}_{l,g},
    \qquad
    \alpha_{l,g}
    =
    \log q^{R}_{l,g}
    -\frac{1}{2}\left(\log q^{\rm in}_{l,g}+\log q^{\rm out}_{l,g}\right).
    \label{eq:pressure-activity}
\end{align}
The pressure contrast $\pi_{l,g}$ compares the two adjacent matrix roles, and
the activity contrast $\alpha_{l,g}$ compares rational-coefficient gradient
energy with the geometric mean of those role memories.  Appendix
\ref{app:matrixpolicy-proxies} derives the local gradient geometry behind these
proxies.  We drop the step subscript when the time is clear.

The second family of summaries comes from the RLB forward-statistics hook.  The
hook samples normalized coordinates and caches per-group response and derivative
RMS values,
\begin{align}
    \hat d^{\rm live}_{l,g}
    =
    \sqrt{\operatorname{mean}\!\left(R'_{l,g}(u_{l,g})^2\right)+\epsilon_{\rm live}},
    \qquad
    \hat o^{\rm live}_{l,g}
    =
    \sqrt{\operatorname{mean}\!\left(R_{l,g}(u_{l,g})^2\right)+\epsilon_{\rm live}}.
    \label{eq:live-gains}
\end{align}
The mean is over sampled scalar normalized coordinates for group $(l,g)$ across
the most recent refreshed sample, sequence positions, and the $m$ group
coordinates.  The derivative gain is the input-role gain proxy, and the response
gain is the output-role gain proxy.  If a cache is unavailable at a step, the
implementation sets the corresponding gain multiplier to identity while keeping
the pressure/activity memories active.

The third family supplies the post-step pair rescale.  Let
$\mathcal{U}=\{u_k\}_{k=1}^{129}$ be a uniform probe grid over $[-5,5]$, and
define
$\rms_{\mathcal{U}}(\varphi)=\sqrt{|\mathcal{U}|^{-1}\sum_{u_k\in\mathcal{U}}\varphi(u_k)^2}$.
The fixed-grid probe producer refreshes every ten optimizer steps and emits
\begin{align}
    \hat d^{\rm probe}_{l,g}
    &=
    \sqrt{\rms_{\mathcal U}(R'_{l,g})^2+\epsilon_{\rm probe}},
    \qquad
    \hat o^{\rm probe}_{l,g}
    =
    \sqrt{\rms_{\mathcal U}(R_{l,g})^2+\epsilon_{\rm probe}}.
    \label{eq:probe-gains}
\end{align}
These probes describe rational curve shape on a shared interval; they are
separate from the sampled live gains in \eqref{eq:live-gains}.  After the
role/depth matrix step and immediately before reciprocal rescaling, the wrapper
also measures the detached pair-state coordinate
\begin{align}
    \gamma_{l,g}
    =
    \log\rms_{\epsilon_p}(A_{l,g})
    -\log\rms_{\epsilon_p}(B_{l,g}).
    \label{eq:post-step-pair-ratio}
\end{align}
Only the probe gains and $\gamma_{l,g}$ are emitted by this summary family.  The
third control then computes $\tau_{l,g}$ and $\chi^{\rm rs}_{l,g}$ from those
summaries and from the pressure and activity memories.

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/matrixpolicy_signal_flow.pdf}
\caption{Detached-summary route for the evaluated MatrixPolicy system.  The
top band produces stop-gradient summaries from forward hooks, pre-policy
gradients, fixed-grid probes, and post-step pair-state measurements; the
read-only bus routes them to three MatrixPolicy consumers.  The lower policy
band separates the gradient/update lane from the current matrix-pair state lane,
while rational coefficients and non-RLB tensors remain AdamW-only.}
\label{fig:matrixpolicy-signal-flow}
\end{figure}

\subsubsection{Group-Gradient Gating}

The first control redistributes gradient scale among groups inside each matrix
role.  It reads the sampled live gains, the pressure contrast $\pi_{l,g}$, and
the activity contrast $\alpha_{l,g}$, then produces scaled gradients for
$A_l$ and $B_l$.  A smooth gate
$\phi_t=\smooth(0.02,0.30,\xi_t)$ turns on this control in the reported runs.
The evaluated manifest sets gain, pressure, and activity strengths to $0.20$,
$0.10$, and $0.20$, and clips group multipliers to $[0.75,1.35]$.  For positive
group values $v_j$, define
$\geom_{j=1}^{G}(v_j)=\exp\!\left(G^{-1}\sum_{j=1}^{G}\log v_j\right)$.  The
group-gradient rule is
\begin{equation}
\begin{alignedat}{2}
    \tilde{\pi}_{l,g}
    &=
    \clip(\pi_{l,g},-1.50,1.50),
    \qquad&
    c^{\rm activity}_{l,g}
    &=
    \exp\!\left[
    -0.20\phi_t
    \relu\!\left(\frac{\alpha_{l,g}-0.05}{0.45}\right)
    \right],
    \\[2pt]
    c^{\rm gain}_{l,g,\mathrm{in}}
    &=
    \left(
    \frac{\geom_{j=1}^{G}\hat d^{\rm live}_{l,j}}
         {\hat d^{\rm live}_{l,g}}
    \right)^{0.20\phi_t},
    &
    c^{\rm gain}_{l,g,\mathrm{out}}
    &=
    \left(
    \frac{\geom_{j=1}^{G}\hat o^{\rm live}_{l,j}}
         {\hat o^{\rm live}_{l,g}}
    \right)^{0.20\phi_t},
    \\[2pt]
    c^{\rm pressure}_{l,g,\mathrm{in}}
    &=
    \exp(-0.10\phi_t\tilde{\pi}_{l,g}),
    &
    c^{\rm pressure}_{l,g,\mathrm{out}}
    &=
    \exp(+0.10\phi_t\tilde{\pi}_{l,g}),
    \\[2pt]
    c^{\rm raw}_{l,g,\sigma}
    &=
    c^{\rm gain}_{l,g,\sigma}
    c^{\rm pressure}_{l,g,\sigma}
    c^{\rm activity}_{l,g},
    &
    c_{l,g,\sigma}
    &=
    \clip\!\left(
    \frac{c^{\rm raw}_{l,g,\sigma}}
         {\geom_{j=1}^{G}c^{\rm raw}_{l,j,\sigma}},
    0.75,1.35
    \right).
\end{alignedat}
\label{eq:group-policy}
\end{equation}
Live derivative gain controls the input role, live response gain controls the
output role, and pressure acts with opposite signs on the two roles.  When
$\pi_{l,g}>0$, the input-side relative-gradient EMA is larger than the
output-side EMA, so the gate reduces the input-role gradient scale and increases
the output-role gradient scale for that group.  The activity term damps groups
whose rational coefficient gradients are large relative to the adjacent matrix
memories.  The geometric recentering preserves the role-wise mean log multiplier
before clipping; Appendix~\ref{app:matrixpolicy-role-gate} gives the log-space
identity.

The scaled matrix gradients are
\begin{align}
    \widetilde\nabla A_{l,g}=c_{l,g,\mathrm{in}}\nabla A_{l,g},
    \qquad
    \widetilde\nabla B_{l,g}=c_{l,g,\mathrm{out}}\nabla B_{l,g}.
    \label{eq:scaled-gradients}
\end{align}
Let $\widetilde\nabla M_{l,\mathrm{in}}$ be the row-block concatenation of
$\{\widetilde\nabla A_{l,g}\}_{g=1}^{G}$, and let
$\widetilde\nabla M_{l,\mathrm{out}}$ be the column-block concatenation of
$\{\widetilde\nabla B_{l,g}\}_{g=1}^{G}$.

\subsubsection{Role/Depth Matrix Step}

The second control applies the matrix optimizer step to the scaled gradients.
It assigns different depth trends to the input and output roles, runs AdamW on
both matrices, and activates a transient Muon substep early in training.  In the
reported configuration, the AdamW multiplier depends on role and depth; the
pressure and activity memories enter this control through the Muon attenuation
factor.  Let $\delta_l=(l-1)/(L-1)$ for $L>1$ and $\delta_l=1/2$ otherwise.  The
branch schedules are
\begin{equation}
\begin{aligned}
    \varrho_{\mathrm{in}}(l)
    &=
    \clip(1-0.50(\delta_l-0.5),0.55,1.40),
    \\
    \varrho_{\mathrm{out}}(l)
    &=
    \clip(1+1.00(\delta_l-0.5),0.55,1.40),
    \\[2pt]
    s^{\rm Adam}_{l,\sigma}
    &=
    \clip\!\left(
    3.0\max\{0.10,1+1.20(\varrho_\sigma(l)-1)\},
    0.40,4.0
    \right),
    \\[2pt]
    \Psi_{l,t}
    &=
    \clip\!\left(G^{-1}\sum_{g=1}^{G}|\pi_{l,g}|,0,1.50\right),
    \\
    \Omega_{l,t}
    &=
    \relu\!\left(\frac{G^{-1}\sum_{g=1}^{G}\alpha_{l,g}-0.05}{0.45}\right),
    \\[2pt]
    \psi_{l,t}
    &=
    \clip\!\left(\exp[-0.30\Psi_{l,t}-0.65\Omega_{l,t}],0.10,1.15\right),
    \\[2pt]
    \omega^{\mu}_t
    &=
    \smooth(0.02,0.12,\xi_t)
    [1-\smooth(0.20,0.36,\xi_t)],
    \\[2pt]
    \mu_{l,\sigma,t}
    &=
    \clip\!\left(
    0.75\,\omega^{\mu}_t\varrho_\sigma(l)\psi_{l,t},
    0,0.75
    \right).
\end{aligned}
\label{eq:matrix-branch-schedule}
\end{equation}
Here $s^{\rm Adam}_{l,\sigma}$ is the role/depth AdamW multiplier.  The aggregate
pressure $\Psi_{l,t}$ and activity $\Omega_{l,t}$ form the Muon attenuation
$\psi_{l,t}$, so the active Muon fraction $\mu_{l,\sigma,t}$ is largest during
the early Muon window and shrinks when the detached summaries indicate large
role imbalance or large rational-coefficient activity.
For $M_{l,\mathrm{in}}=A_l$ and $M_{l,\mathrm{out}}=B_l$, let $\eta_t$ be the
base learning rate and let $s_\mu=1.0$ be the evaluated Muon learning-rate scale.
MatrixPolicy applies an AdamW matrix step followed by any active Muon substep.
The gate $\mu_{l,\sigma,t}$ scales the two optimizer learning rates; AdamW and
Muon keep separate states and are applied sequentially:
\begin{equation}
\begin{gathered}
    \widetilde M_{l,\sigma}^{\,t+1}
    =
    \operatorname{AdamW}_{\eta_t s^{\rm Adam}_{l,\sigma}(1-\mu_{l,\sigma,t})}
    (M_{l,\sigma}^{t};\widetilde\nabla M_{l,\sigma}^{t}),\\
    M_{l,\sigma}^{t+1}
    =
    \operatorname{Muon}_{\eta_t s_\mu \mu_{l,\sigma,t}}
    (\widetilde M_{l,\sigma}^{\,t+1};\widetilde\nabla M_{l,\sigma}^{t}),
\end{gathered}
   \label{eq:matrix-branch-step}
\end{equation}
The AdamW step uses the training-protocol betas, weight decay, and
learning-rate schedule in Appendix~\ref{app:experiment-protocol}.  The Muon
substep is the \texttt{torch.optim.Muon} update on two-dimensional RLB matrices
with momentum $0.95$, five Newton--Schulz steps, and match-RMS learning-rate
adjustment \citep{liu2025muon}; it becomes inactive when all
$\mu_{l,\sigma,t}$ decay to zero.  Appendix~\ref{app:matrixpolicy-muon-control}
states the matrix-spectrum geometry behind this transient branch.

\subsubsection{Reciprocal Pair Rescaling}

The third control chooses a bounded positive representative of the same RLB
matrix pair after the matrix update.  It is implemented by the surrounding
transport wrapper and is evaluated every five optimizer steps after the
AdamW/Muon branch; fixed-grid probe gains are refreshed every ten optimizer
steps and reused between refreshes.  It consumes the probe gains, the post-step
log ratio $\gamma_{l,g}$, and the pressure/activity memories.  The target
combines the probe-gain balance coordinate derived in
Appendix~\ref{app:matrixpolicy-pair-rescale} with a bounded pressure offset:
\begin{equation}
\begin{aligned}
    \bar{\pi}_{l,g}
    &=
    \clip(\pi_{l,g},-1.25,1.25),
    \\[2pt]
    \tau_{l,g}
    &=
    \frac{1}{2}
    \left(
    \log \hat d^{\rm probe}_{l,g}
    -
    \log \hat o^{\rm probe}_{l,g}
    \right)
    +0.25\,\bar{\pi}_{l,g},
    \\
    \bar{\alpha}_{l,g}
    &=
    \clip(\alpha_{l,g},-1.25,1.25),
    \\[2pt]
    \chi^{\rm rs}_{l,g}
    &=
    \exp(0.10\,\bar{\alpha}_{l,g}),
    \qquad \text{(rescale-speed factor)}
    \\[2pt]
    s_{l,t}
    &=
    0.50\,\smooth(0.03,0.35,\xi_t)
    \clip(1+0.15(\delta_l-0.5),0.75,1.25).
\end{aligned}
\label{eq:rescale-signals}
\end{equation}
The current log ratio $\gamma_{l,g}$ is measured after the matrix step.  The
clipped log step and reciprocal update are
\begin{equation}
\begin{aligned}
    \ell_{l,g,t}
    &=
    \clip\!\left(
    \frac{1}{2}[\tau_{l,g}-\gamma_{l,g}]s_{l,t}\chi^{\rm rs}_{l,g},
    -0.030,0.030
    \right),
    \\
    A_{l,g}
    &\leftarrow
    e^{\ell_{l,g,t}}A_{l,g},
    \qquad
    B_{l,g}
    \leftarrow
    e^{-\ell_{l,g,t}}B_{l,g}.
\end{aligned}
\label{eq:rescale-step}
\end{equation}
The first term of $\tau_{l,g}$ equalizes derivative and response probe gains in
the positive log coordinate, the pressure term shifts the target within a
bounded range, and $\chi^{\rm rs}$ changes the clipped rescale speed.  Larger
$\alpha_{l,g}$ accelerates this representative-balancing move, while the earlier
gradient gate and Muon attenuation use the same activity contrast to reduce
gradient-driven matrix motion.  In the nonzero, unfloored idealization of
\eqref{eq:positive-rescale}, the reciprocal pair move preserves the represented
function; near the RMS floor it is a clipped approximate balance step.

\FloatBarrier
\section{Experiments}
\label{sec:experiments}

\subsection{Fixed-Scale Target-Arrival Study}
\label{sec:fixed-scale-results}

Recent optimizer studies show that practical rankings depend on compute-time
accounting, controlled tuning, standardized pretraining scenarios, and model or
data scale \citep{shah2025practicalefficiency,semenov2025benchmarking,
wen2026fantastic,volkova2026optimizerlaws}.  The fixed-scale study therefore uses
matched models, data, budgets, and loss targets while reporting both token and
wall-clock arrival.

The fixed-scale study uses the same 12-layer, width-768 causal Transformer on
five corpora: DCLM/DataComp-LM \citep{li2024datacomp}, FineWeb-Edu and FineWeb
\citep{penedo2024fineweb}, Dolma-sample \citep{soldaini2024dolma}, and C4
\citep{raffel2020t5}.  The 100M-token budget uses 3050 optimizer steps and
the 300M-token budget uses 9150 optimizer steps, with three seeds per
method/dataset pair.  The main comparison keeps the architecture and data fixed
while changing the activation and optimizer used in the nonlinear sublayer:
MatrixPolicy is compared with SiLU controls and with RLB-only controls, where
RLB-only places the same activation under the named standard optimizer.
Shared optimizer, evaluation-cadence, and
throughput-measurement details are reported in
Appendix~\ref{app:experiment-protocol}; broader optimizer comparisons beyond
AdamW and Muon are summarized in Table~\ref{tab:broad-final-validation}.

\input{tables/e1_e2_silu_summary_table.tex}
\FloatBarrier

Table~\ref{tab:e1e2-silu-comparison} is the primary empirical result.  It asks:
for the hardest common validation-loss target reached by every listed method in
all three seeds, what fraction of target-arrival tokens does MatrixPolicy save,
and how many measured target-arrival minutes does each method require?  Across all
five datasets under the 100M-token budget, MatrixPolicy saves 19.5--22.8\% of
the SiLU with AdamW target-arrival tokens and 29.2--34.0\% of the SiLU with
Muon target-arrival tokens.  Under the 300M-token budget, after longer training
compresses endpoint gaps, MatrixPolicy still saves 22.3--26.4\% versus SiLU
with AdamW and 6.7--9.8\% versus SiLU with Muon, with lower target-arrival time
in every listed comparison.

\begin{figure}[H]
\centering
\includegraphics[width=0.99\textwidth]{figures/target_arrival_evidence_matrix.pdf}
\caption{MatrixPolicy shifts target arrival left across the completed fixed-scale
study.  Rows show the 100M- and 300M-token budgets, and columns show datasets.
Each point is the mean token count needed to reach one validation-loss target in
all three seeds; connected points trace one method from easier to harder targets.
Black circles are MatrixPolicy.  For every optimizer color, solid squares are
RLB with that optimizer and dashed triangles are SiLU with the same optimizer.
Lower targets are harder, and the hard-target timing and token-saving
percentages are quantified in Table~\ref{tab:e1e2-silu-comparison}.}
\label{fig:target-arrival-evidence}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.98\textwidth]{figures/e1_representative_silu_dynamics.pdf}
\caption{Representative 100M-token-budget training dynamics on DCLM and
FineWeb-Edu.  Rows show datasets and columns show validation loss, validation
perplexity, and training loss.  Lines are seed means over three runs and bands
show one sample standard deviation.  This figure uses the two standard SiLU
baselines so that the early target-arrival effect in
Table~\ref{tab:e1e2-silu-comparison} remains visually legible; inset windows
zoom the final-quarter saturation region.}
\label{fig:e1-representative-dynamics}
\end{figure}

The 100M-token-budget curves show why target arrival is the right main readout.
MatrixPolicy separates early in validation loss and validation perplexity while
keeping training loss competitive.  A long fixed budget eventually compresses
many activation/optimizer endpoints, especially under the 300M-token budget,
so the paper emphasizes the number of tokens and minutes needed to reach the same
useful validation-loss levels.

\FloatBarrier

\subsection{TODO: Larger-Scale Training Results}
\label{sec:todo-larger-scale}

TODO: Insert the larger-scale training result here.

\subsection{TODO: Component Ablations}
\label{sec:todo-ablations}

TODO: Insert the component ablation study here.


\section{Conclusion}
\label{sec:conclusion}

RLB turns the nonlinear Transformer sublayer into an optimizer-visible interface
by exposing grouped normalized coordinates, trainable groupwise rational
responses, and an idealized positive matrix-pair relation used as a bounded
balancing coordinate.  MatrixPolicy uses that interface to update the
RLB input and output matrices with role-aware statistics, while rational
coefficients and non-RLB-matrix parameters use AdamW.  The completed 100M-token
and 300M-token runs show that MatrixPolicy reaches common validation losses with
fewer tokens and lower measured
target-arrival time than the listed SiLU and RLB controls.  [todo: Complete this
conclusion after the larger-scale and ablation results are added.]

\bibliography{references}
\bibliographystyle{iclr2026_conference}

\appendix



\section{Experiment Appendix}
\label{app:experiments}

This appendix gives the fixed-scale protocol behind
Section~\ref{sec:fixed-scale-results}, the full training curves for the five
corpora, and the broader optimizer endpoint comparison used to contextualize the
main SiLU and RLB controls.

\subsection{Fixed-Scale Target-Arrival Study}
\label{app:experiment-protocol}

\subsubsection{Model and Data Pipeline}
\label{app:model-data}

The fixed-scale experiments use a custom causal decoder Transformer.  The block
uses a LLaMA-style pre-normalized layout with RMSNorm before attention and before
the nonlinear sublayer, bias-free query/key/value and output attention
projections, rotary position embeddings, causal scaled-dot-product attention,
residual attention and nonlinear-sublayer updates, a final RMSNorm, and a tied
token-embedding/output head \citep{zhang2019rmsnorm,su2021roformer,touvron2023llama}.
The fixed model has 12 layers, width 768, 12 heads of dimension 64, and context
length 256.

The SiLU controls use a SwiGLU-like gated nonlinear sublayer with bias-free
matrices $W_g,W_v\in\mathbb{R}^{768\times 2048}$ and
$W_o\in\mathbb{R}^{2048\times 768}$, computing
$(\operatorname{SiLU}(xW_g)\odot xW_v)W_o$.  MatrixPolicy and RLB-only
configurations use a matched single-branch matrix pair.  An input matrix maps
width 768 to 3072 latent coordinates, the coordinates are partitioned into
groups and transformed by the real-pole-free P5/Q4 rational response, and an
output matrix maps the 3072 coordinates back to width 768.  With the default
group size 256, this gives $G=12$ groups of width $m=256$; the RMS floor is
$\epsilon_{\rm rms}=10^{-6}$.  This is the RLB configuration used by the main
MatrixPolicy and RLB-control results.

Tokenization uses the GPT-2 tokenizer through the Hugging Face tokenizer
interface with its base vocabulary; an EOS or pad token is appended between
documents.  Training samples random contiguous blocks of
length 257 and forms next-token prediction pairs of length 256.  The DCLM,
FineWeb-Edu, FineWeb, Dolma-sample, and C4 runs use the text field specified in
the manifests, with validation slices drawn from held-out token offsets of the
same corpus stream.

\subsubsection{Training Protocol}
\label{app:training-protocol}

All runs use batch size 16, gradient accumulation 2, and therefore 32768 global
tokens per optimizer step.  The 100M-token budget consists of 3050 optimizer
steps; the 300M-token budget consists of 9150 optimizer steps.  Each
method/dataset pair is run with seeds 1337, 2027, and 3407.  Validation is
performed every 50 optimizer steps, so token-to-target arrivals are quantized in
increments of 1.64M tokens.

The MatrixPolicy rows in the main table use the RLB activation, whose active
nonlinearity is the groupwise P5/Q4 response in
equations~\ref{eq:rlb-p}--\ref{eq:rlb-response}.  The
evaluated configuration uses AdamW as the backbone optimizer and disables the
separate rational-coefficient optimizer.  It enables MatrixPolicy group
redistribution with gain/pressure/activity strengths $0.20/0.10/0.20$, schedule
window $0.02$--$0.30$, and group multiplier clip $[0.75,1.35]$.  The RLB matrix
pair then uses the role/depth-scaled AdamW branch and the early Muon substep from
Section~\ref{sec:method-update}.  The bounded RLB pair-rescale wrapper runs
every five optimizer steps after the matrix branch.  Rational coefficients,
attention weights, embeddings, normalization parameters, and ordinary
Transformer weights use AdamW in this configuration.

Target-arrival minutes in Table~\ref{tab:e1e2-silu-comparison} are computed
seed-wise from the first validation event that reaches the shared target and the
same run's measured seconds per optimizer step.  The per-step time is the
training-script wall time recorded in the run summary divided by completed
optimizer steps for that run; target arrivals are therefore measured with the
same evaluation cadence and timing source across methods.

\subsubsection{Supporting Fixed-Scale Curves}
\label{app:supporting-curves}

\begin{figure}[H]
\centering
\includegraphics[width=0.99\textwidth]{figures/e1_multimetric_all_datasets.pdf}
\caption{100M-token-budget dynamics across all five datasets.  Rows are datasets
and columns are validation loss, validation perplexity, and training loss.  Lines
are seed means over three runs and bands show one sample standard deviation.
Insets zoom the final-quarter saturation region in each panel.}
\label{fig:appendix-100m-multimetric}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.99\textwidth]{figures/e2_multimetric_all_datasets.pdf}
\caption{300M-token-budget dynamics across all five datasets.  Rows are datasets
and columns are validation loss, validation perplexity, and training loss.  Lines
are seed means over three runs and bands show one sample standard deviation.
Insets zoom the middle training window where early saturation differences are
most visible.}
\label{fig:appendix-300m-multimetric}
\end{figure}

\subsubsection{Broad Optimizer Endpoint Table}
\label{app:broad-optimizer-table}

The fixed-scale package also includes the broader optimizer endpoints referenced
from Section~\ref{sec:fixed-scale-results}.  These rows use the same model,
datasets, seeds, token budgets, and validation schedule as the main comparison,
while replacing the nonlinear-sublayer optimizer according to the table entry.

\input{tables/final_validation_broad_optimizer_table.tex}
\FloatBarrier

\subsubsection{Learning-Rate and Weight-Decay Sensitivity}
\label{app:lr-wd-sensitivity}

We ran a 100M-token, single-seed sensitivity grid for MatrixPolicy and the two
standard SiLU controls.  The grid crosses learning rates
$\{1,2,3,5\}\times10^{-4}$ with weight decays $0$, $0.05$, $0.10$, and $0.20$
on all five corpora, using the model, tokenizer, evaluation cadence, and token
budget in Appendix~\ref{app:training-protocol}.  Let $d$ index a corpus,
$\eta$ denote the outer learning rate, $\lambda$ the weight-decay coefficient,
$h=(\eta,\lambda)$ their matched grid cell, and
$L_{d,h,a}(s_k)$ be method $a$'s validation loss at
$s_k\in\mathcal S=\{50,100,\ldots,3050\}$.  Writing $\mathrm{MP}$ for
MatrixPolicy and $c$ for either SiLU control, define the observed-checkpoint
margin
\begin{equation}
    m_{d,h,c}
    =
    \min_{s_k\in\mathcal S}
    \left[
        L_{d,h,c}(s_k)-L_{d,h,\mathrm{MP}}(s_k)
    \right].
    \label{eq:e8-trajectory-margin}
\end{equation}
A positive margin certifies that MatrixPolicy has lower validation loss at
every observed checkpoint from step 50 onward in that matched cell.
For a target $\ell$, define its sampled first-hit token count as
\begin{equation}
    N_{d,h,a}(\ell)
    =
    32{,}768\min\{s_k\in\mathcal S:L_{d,h,a}(s_k)\le\ell\},
    \label{eq:e8-first-hit-tokens}
\end{equation}
with $N_{d,h,a}(\ell)=+\infty$ when the set is empty.  Therefore
$m_{d,h,c}>0$ implies
$N_{d,h,\mathrm{MP}}(\ell)\le N_{d,h,c}(\ell)$ whenever the control's first hit
is finite.

\begin{figure}[!t]
    \centering
    \includegraphics[width=0.99\textwidth]{figures/e8_lr_wd_trajectory_dominance.pdf}
    \caption{Paired observed-checkpoint margins over the complete
    learning-rate/weight-decay grid.  Each tile is
    $m_{d,h,c}$ from equation~\ref{eq:e8-trajectory-margin} for one dataset,
    grid cell, and comparator at seed 1337.  Positive values mean that
    MatrixPolicy has lower validation loss at every observed checkpoint from
    steps 50 through 3050.  The dark outline marks the fixed main-study
    setting $(3\times10^{-4},0.10)$; all 160 tiles are positive.}
    \label{fig:e8-trajectory-dominance}
\end{figure}

Figure~\ref{fig:e8-trajectory-dominance} gives strict observed-checkpoint
dominance in all 80 dataset--grid cells against each control.  Every one of the
4,880 paired checkpoint comparisons per control is positive; the smallest
observed margin is $0.041$ against SiLU with AdamW and $0.045$ against SiLU with
Muon.

Table~\ref{tab:e8-sensitivity-target-arrival} makes this target-arrival
consequence explicit at the main 100M-token targets from
Table~\ref{tab:e1e2-silu-comparison} and at targets $0.10$ and $0.20$ loss units
lower.  Each row partitions all 16 cells and reports mutual non-arrivals as a
separate outcome.

\input{tables/e8_sensitivity_target_arrival_table.tex}

At the main targets, MatrixPolicy arrives earlier in all 40 mutual-arrival
comparisons per control and is the only method to arrive in 20 additional
cells; no cell favors a control.  The same ordering holds at both harder target
levels.  Pooled over mutual arrivals, median token savings at the main,
$-0.10$, and $-0.20$ target levels are $15.7\%$, $19.3\%$, and $14.3\%$ against
SiLU with AdamW and $19.9\%$, $13.5\%$, and $10.3\%$ against SiLU with Muon.
Node assignments were not matched across the sensitivity sweep, so this
analysis reports token arrivals only.  The primary-study wall-clock comparison
remains in Table~\ref{tab:e1e2-silu-comparison}.  Together, the checkpoint and
target analyses show the same convergence advantage at all 16 evaluated
learning-rate and weight-decay settings for seed 1337.
\FloatBarrier

\subsection{TODO: Larger-Scale Training Results}
\label{app:todo-larger-scale}

TODO: Insert larger-scale training details here.

\subsection{TODO: Component Ablations}
\label{app:todo-ablations}

TODO: Insert component ablation details here.


\section{Rational Latent Basis Calculus}
\label{app:rlb-calculus}

The RLB summaries and the reciprocal matrix-pair coordinate follow from the
scalar response and the RMS-normalized group map.  We suppress layer and group
indices throughout this section.

\subsection{Pole-Free Rational Response}
\label{app:rlb-response-derivatives}

Let $\boldsymbol\phi(u)$ collect the nonconstant denominator features.  The
evaluated P5/Q4 response is
\begin{equation}
\begin{alignedat}{2}
    P(u)
    &:=\sum_{i=0}^{5}a_i u^i,
    &\qquad
    \boldsymbol\phi(u)
    &:=\bigl(|u|,u^2,|u|^3,u^4\bigr),
    \\
    Q(u)
    &:=1+\sum_{j=1}^{4}|b_j|\phi_j(u),
    &
    R(u)
    &:=\frac{P(u)}{Q(u)}.
\end{alignedat}
    \label{eq:appendix-local-pq}
\end{equation}
Because each denominator feature is nonnegative, $Q(u)\ge1$ for every
$u\in\mathbb R$; hence the response is pole-free on $\mathbb R$.  Away from the kink at
$u=0$,
\begin{equation}
\begin{aligned}
    R'(u)
    &=\frac{P'(u)Q(u)-P(u)Q'(u)}{Q(u)^2},
    \\
    P'(u)
    &=\sum_{i=1}^{5} i a_i u^{i-1},
    \qquad
    Q'(u)
    =|b_1|\sign(u)+2|b_2|u+3|b_3|u|u|+4|b_4|u^3.
\end{aligned}
    \label{eq:appendix-r-prime}
\end{equation}
The selected coefficient derivatives are
\begin{equation}
\begin{aligned}
    \frac{\partial R(u)}{\partial a_i}
    &=\frac{u^i}{Q(u)},
    &
    \frac{\partial R(u)}{\partial b_j}
    &=-\frac{P(u)}{Q(u)^2}\sign(b_j)\phi_j(u).
\end{aligned}
    \label{eq:appendix-coeff-features}
\end{equation}
The implementation selects $\sign(0)=0$ for both $u=0$ and $b_j=0$.  At these
kinks, the formulas specify the backward derivative chosen by autodiff.  For
vector inputs, $R$ and $R'$ act
coordinatewise.  The response, derivative, and coefficient-activity summaries
therefore originate from $R(u)$, $R'(u)$, and the features in
\eqref{eq:appendix-coeff-features}, respectively.  For the evaluated response,
$\Theta^R_{l,g}=\{a_{l,g,\cdot},b_{l,g,\cdot}\}$ in
\eqref{eq:rational-activity-raw}.

\subsection{RMS-Normalized Group Differential}
\label{app:rlb-group-differential}

Let $m$ be the group width and let $\epsilon_{\rm rms}\ge0$.  For
$\epsilon_{\rm rms}>0$ and $z\in\mathbb R^m$, or for
$\epsilon_{\rm rms}=0$ and $z\ne0$, define
\begin{equation}
    r(z)=\sqrt{m^{-1}\lVert z\rVert_2^2+\epsilon_{\rm rms}},
    \qquad
    u(z)=\frac{z}{r(z)},
    \qquad
    h(z)=r(z)R(u(z)).
\end{equation}
On the domain $r(z)>0$, direct differentiation gives
\begin{equation}
\begin{aligned}
    \mathrm{d}r
    &=
    \frac{z^\top\mathrm{d}z}{mr}
    =
    \frac{u^\top\mathrm{d}z}{m},
    &
    \mathrm{d}u
    &=
    \frac{1}{r}
    \left(I_m-\frac{uu^\top}{m}\right)\mathrm{d}z .
\end{aligned}
    \label{eq:appendix-du}
\end{equation}
The factor $I_m-uu^\top/m$ is the exact normalized-coordinate differential.  It
is the tangent projector to the RMS sphere when $\epsilon_{\rm rms}=0$.  With a
positive floor, its radial eigenvalue is $\epsilon_{\rm rms}/r^2$.  Away from
scalar-response kinks, the group Jacobian is
\begin{equation}
    J_h(z)
    :=
    \frac{\partial h}{\partial z}
    =
    R(u)\frac{u^\top}{m}
    +
    \diag(R'(u))
    \left(I_m-\frac{uu^\top}{m}\right).
    \label{eq:appendix-group-jacobian}
\end{equation}
This decomposition identifies the scalar factors observed by the live
summaries.  Response RMS summarizes the radial response factor, and derivative
RMS summarizes $\diag(R'(u))$.  The complete local Jacobian additionally
depends on the normalized-coordinate factor and radial term; the matrix-role
gradient also depends on the output matrix and upstream adjoint.  The two RMS
summaries therefore serve as role-specific scalar proxies.

\subsection{Positive Pair Coordinate and Floor Perturbation}
\label{app:rlb-positive-coordinate}

Set $\epsilon_{\rm rms}=0$ and write $r_0,u_0,h_0$ for the unfloored map.  For
$z\ne0$ and $\lambda>0$,
\begin{equation}
    r_0(\lambda z)=\lambda r_0(z),
    \qquad
    u_0(\lambda z)=u_0(z),
    \qquad
    h_0(\lambda z)=\lambda h_0(z).
\end{equation}
For a group contribution $y_{l,g}=B_{l,g}h(A_{l,g}x_l)$, with $R$ fixed,
$\epsilon_{\rm rms}=0$, and $A_{l,g}x_l\ne0$, the transformation
$(A_{l,g},B_{l,g})\mapsto
(\lambda A_{l,g},\lambda^{-1}B_{l,g})$ leaves $y_{l,g}$ unchanged.  This is
the positive pair orbit in \eqref{eq:positive-rescale}.

The RMS floor perturbs the orbit through both the radius and normalized
coordinate.  Write
\begin{equation}
\begin{aligned}
    r_\epsilon(z)
    &=\sqrt{m^{-1}\lVert z\rVert_2^2+\epsilon_{\rm rms}},
    &
    u_\epsilon(z)
    &=z/r_\epsilon(z),
    &
    h_\epsilon(z)
    &=r_\epsilon(z)R(u_\epsilon(z)).
\end{aligned}
\end{equation}
For $\rho^2=m^{-1}\lVert z\rVert_2^2>0$ and
$\delta=\epsilon_{\rm rms}/\rho^2$, define
\begin{equation}
\begin{aligned}
    s_\epsilon(\lambda,z)
    &:=
    \frac{r_\epsilon(\lambda z)}{\lambda r_\epsilon(z)}
    =
    \sqrt{\frac{1+\delta/\lambda^2}{1+\delta}},
    &
    \kappa(\lambda,z)
    &:=
    s_\epsilon(\lambda,z)^{-1},
    \\
    u_\epsilon(\lambda z)
    &=\kappa(\lambda,z)u_\epsilon(z).
\end{aligned}
    \label{eq:appendix-kappa}
\end{equation}
Fix $c>0$, and let $\mathcal I_c$ contain every scalar coordinate on all
segments joining $u_\epsilon(z)$ and
$\kappa(\lambda,z)u_\epsilon(z)$ for
$\lambda\in[e^{-c},e^c]$ and all sufficiently small $\delta$.  Suppose that
$R$ is $L_R$-Lipschitz on $\mathcal I_c$.  Reciprocal output compensation then
gives, for every $\lambda$ in this interval, the latent feature bound
\begin{equation}
\begin{aligned}
    \left\|\lambda^{-1}h_\epsilon(\lambda z)-h_\epsilon(z)\right\|_2
    &\le
    \left|\frac{r_\epsilon(\lambda z)}{\lambda}-r_\epsilon(z)\right|
    \left\|R(u_\epsilon(z))\right\|_2
    \\
    &\quad+
    \frac{r_\epsilon(\lambda z)}{\lambda}
    L_R|\kappa(\lambda,z)-1|\left\|u_\epsilon(z)\right\|_2 .
\end{aligned}
    \label{eq:appendix-floor-bound}
\end{equation}
After the pair map, the one-group output difference is
$B_{l,g}[\lambda^{-1}h_\epsilon(\lambda z)-h_\epsilon(z)]$, where
$B_{l,g}$ denotes the pre-rescale output block.  Its norm is at most
$\lVert B_{l,g}\rVert_{\rm op}$ times the right-hand side of
\eqref{eq:appendix-floor-bound}.  For fixed $c$ and fixed $m$, uniformly over
$\lambda\in[e^{-c},e^c]$ as $\delta\to0$,
\begin{equation}
\begin{aligned}
    \sup_{\lambda\in[e^{-c},e^c]}
    \bigl(|s_\epsilon(\lambda,z)-1|+|\kappa(\lambda,z)-1|\bigr)
    &=O_c(\delta),
    \\
    \sup_{\lambda\in[e^{-c},e^c]}
    \left\|\lambda^{-1}h_\epsilon(\lambda z)-h_\epsilon(z)\right\|_2
    &=
    O_c\!\left(
    \sqrt m\,[\sup_{\mathcal I_c}|R|+L_R]\,
    \frac{\epsilon_{\rm rms}}{\rho}
    \right).
\end{aligned}
\end{equation}
The first line follows directly from
$|s_\epsilon-1|\le\tfrac12\delta|\lambda^{-2}-1|$ and
$|\kappa-1|=|s_\epsilon-1|/s_\epsilon$.  For radii separated from zero and
bounded reciprocal moves, the error decays as $\epsilon_{\rm rms}/\rho$; this
rate motivates the clipped log-scale update in MatrixPolicy.

\section{Optimizer Design and MatrixPolicy Geometry}
\label{app:matrixpolicy-step}

Let $\operatorname{sg}(\cdot)$ denote stop-gradient.  MatrixPolicy first forms
detached role summaries, then rescales the current matrix gradients, applies the
AdamW/Muon matrix branch, and finally moves the updated pair along the positive
coordinate from Appendix~\ref{app:rlb-positive-coordinate}.  Its effective
order is
\begin{equation}
\begin{aligned}
    \nabla\mathcal L
    &\longrightarrow
    \operatorname{sg}\!\left(
    q^{\rm in},q^{\rm out},q^R,\pi,\alpha,
    \hat d^{\rm live},\hat o^{\rm live}
    \right)
    \longrightarrow
    (\widetilde\nabla A,\widetilde\nabla B)
    \\
    &\longrightarrow
    (A^{\rm step},B^{\rm step})
    \longrightarrow
    \operatorname{sg}\!\left(
    \hat d^{\rm probe},\hat o^{\rm probe},\gamma
    \right)
    \\
    &\longrightarrow
    (e^\ell A^{\rm step},e^{-\ell}B^{\rm step}).
\end{aligned}
    \label{eq:appendix-matrixpolicy-pipeline}
\end{equation}

\subsection{Local Matrix-Role Geometry}
\label{app:matrixpolicy-proxies}

For one token of one training example, let $\bar y_l=\nabla_{y_l}\mathcal{L}$ and
$J_{l,g}=\partial h_{l,g}/\partial z_{l,g}$ from
\eqref{eq:appendix-group-jacobian}.  Here $x_l$ is the RLB input,
$z_{l,g}=A_{l,g}x_l$, $h_{l,g}=h(z_{l,g})$, and
$y_l=\sum_g B_{l,g}h_{l,g}$.  The adjacent matrix gradients factor as
\begin{align}
    \nabla_{B_{l,g}}\mathcal{L}
    &=
    \bar y_l h_{l,g}^\top,
    &
    \nabla_{A_{l,g}}\mathcal{L}
    &=
    \left(J_{l,g}^\top B_{l,g}^\top\bar y_l\right)x_l^\top .
    \label{eq:appendix-matrix-gradients}
\end{align}
Batch and sequence gradients are sums or averages of these outer-product terms.
The output-role gradient contains the realized feature $h_{l,g}$, whereas the
input-role gradient contains the local Jacobian $J_{l,g}$.  This factorization
supports response RMS and derivative RMS as scalar role proxies.  The full
gradient retains the omitted radius, normalized-coordinate factor, matrix
orientation, data distribution, and upstream adjoint.

The pressure summaries quantify relative gradient scale.  For a pure gradient
step $W^+=W-\eta\mathcal G$, with $W\ne0$ and
$\eta\lVert\mathcal G\rVert_F\le c\lVert W\rVert_F$ for fixed $c<1$,
Taylor expansion gives
\begin{equation}
    \log\rms(W^+)-\log\rms(W)
    =
    -\eta
    \frac{\langle\mathcal{G},W\rangle_F}{\lVert W\rVert_F^2}
    +O_c\!\left(
    \eta^2\frac{\lVert\mathcal{G}\rVert_F^2}{\lVert W\rVert_F^2}
    \right),
    \label{eq:appendix-log-rms-update}
\end{equation}
while Cauchy--Schwarz yields
\begin{equation}
    \left|
    \frac{\langle\mathcal{G},W\rangle_F}{\lVert W\rVert_F^2}
    \right|
    \le
    \frac{\lVert\mathcal{G}\rVert_F}{\lVert W\rVert_F}
    =
    \frac{\rms(\mathcal{G})}{\rms(W)} .
    \label{eq:appendix-pressure-bound}
\end{equation}
Thus $\rms(\mathcal G)/\rms(W)$ bounds the magnitude of the first-order
log-RMS change for a pure gradient step.  The implemented $p^{\rm in}$ and
$p^{\rm out}$ are floored versions of this nonnegative bound, computed from
post-global-clipping gradients before the group policy.  They summarize unsigned
pre-policy gradient scale; AdamW and Muon subsequently construct signed,
state-dependent updates.  Their EMA contrast $\pi_{l,g}$ identifies the role
with larger recent relative gradient scale.  The activity contrast
$\alpha_{l,g}$ compares raw rational-coefficient gradient energy with the
geometric mean of the matrix-pressure memories and supplies a calibrated policy
signal for relative coefficient activity.

In the unfloored fixed-response map, the positive pair orbit also satisfies
\begin{equation}
    \left.
    \frac{\mathrm d}{\mathrm ds}
    \mathcal L(e^sA_{l,g},e^{-s}B_{l,g})
    \right|_{s=0}
    =
    \langle\nabla A_{l,g},A_{l,g}\rangle_F
    -
    \langle\nabla B_{l,g},B_{l,g}\rangle_F
    =0.
\end{equation}
The final control follows this zero-loss tangent in the idealized map to alter
relative matrix pressure.

\subsection{Centered Group Control}
\label{app:matrixpolicy-role-gate}

The first control is most transparent in log space.  Define the layerwise mean
log gains
\begin{equation}
\begin{aligned}
    \bar d_l
    &=
    \frac{1}{G}\sum_{j=1}^{G}\log \hat d^{\rm live}_{l,j},
    &
    \bar o_l
    &=
    \frac{1}{G}\sum_{j=1}^{G}\log \hat o^{\rm live}_{l,j}.
\end{aligned}
\end{equation}
The gain factors in \eqref{eq:group-policy} obey
\begin{equation}
\begin{aligned}
    \log c^{\rm gain}_{l,g,\mathrm{in}}
    &=
    -0.20\phi_t(\log\hat d^{\rm live}_{l,g}-\bar d_l),
    &
    \log c^{\rm gain}_{l,g,\mathrm{out}}
    &=
    -0.20\phi_t(\log\hat o^{\rm live}_{l,g}-\bar o_l).
\end{aligned}
    \label{eq:appendix-gain-centering}
\end{equation}
Above-geometric-mean derivative or response gain therefore reduces the
corresponding role multiplier.  The pressure factors act with opposite signs,
\begin{equation}
\begin{aligned}
    \log c^{\rm pressure}_{l,g,\mathrm{in}}
    &=
    -0.10\phi_t\,\tilde\pi_{l,g},
    &
    \log c^{\rm pressure}_{l,g,\mathrm{out}}
    &=
    +0.10\phi_t\,\tilde\pi_{l,g},
\end{aligned}
    \label{eq:appendix-pressure-signs}
\end{equation}
so positive $\pi_{l,g}$ reduces the input-role gradient and increases the
output-role gradient before recentering.  The shared activity factor is
\begin{equation}
    \log c^{\rm activity}_{l,g}
    =
    -0.20\phi_t
    \relu\!\left(\frac{\alpha_{l,g}-0.05}{0.45}\right).
    \label{eq:appendix-activity-damping}
\end{equation}
To distinguish the stages of the implementation, let
\begin{equation}
\begin{aligned}
    c^{\rm pre}_{l,g,\sigma}
    &:=
    c^{\rm gain}_{l,g,\sigma}
    c^{\rm pressure}_{l,g,\sigma}
    c^{\rm activity}_{l,g},
    \\
    \widehat c_{l,g,\sigma}
    &:=
    \frac{c^{\rm pre}_{l,g,\sigma}}
         {\geom_{j=1}^{G}c^{\rm pre}_{l,j,\sigma}},
    &
    c_{l,g,\sigma}
    &:=
    \clip(\widehat c_{l,g,\sigma},0.75,1.35),
    \\
    \frac{1}{G}\sum_{g=1}^{G}\log \widehat c_{l,g,\sigma}=0 .
\end{aligned}
    \label{eq:appendix-centered-policy}
\end{equation}
The zero-mean identity holds exactly before the final clip; the clip may shift
the layerwise mean log multiplier.  The gate preserves each group-gradient
direction and multiplies its magnitude by a positive scalar before AdamW and
Muon construct their state-dependent update directions.

\subsection{Transient Spectral Normalization}
\label{app:matrixpolicy-muon-control}

The role/depth factors in \eqref{eq:matrix-branch-schedule} are bounded scalar
preconditioners.  They preserve each matrix direction while changing the step
magnitude assigned to the input and output maps across depth.  AdamW then
maintains entrywise moments
\citep{kingma2015iclr-adam,loshchilov2019decoupled},
\begin{equation}
\begin{aligned}
    M &\leftarrow \beta_1M+(1-\beta_1)G,
    &
    V &\leftarrow \beta_2V+(1-\beta_2)(G\odot G),
    \\
    D^{\rm Adam}_{ij}
    &=
    \frac{M_{ij}}{\sqrt{V_{ij}}+\epsilon},
\end{aligned}
    \label{eq:appendix-adam-coordinate}
\end{equation}
where the display suppresses bias correction and decoupled weight decay.  This
normalization acts coordinatewise; the subsequent Muon branch controls the
singular spectrum of the matrix direction.

Matrix spectra matter for the RLB pair because
\eqref{eq:appendix-matrix-gradients} gives single-example adjacent gradients as
outer products; after averaging over batch and sequence positions, the training
gradients are sums of such terms.  Singular modes of $B_l$ couple
output-residual directions to latent features, and singular modes of $A_l$
couple latent coordinates to input-residual directions.  During early basis
formation, a highly anisotropic singular spectrum concentrates movement in a
narrow set of these coupled directions.

Let $H_{l,\sigma,t}=U\Sigma V^\top$ be the compact SVD of the tensor passed
to Newton--Schulz after group-gradient scaling and Muon's momentum/Nesterov
construction.  Its ideal polar direction is
\begin{equation}
\begin{aligned}
    \mathcal{P}(H_{l,\sigma,t})=UV^\top,
    &
    H=\sum_i\sigma_i u_iv_i^\top
    \mapsto
    \sum_i u_iv_i^\top
    \quad\text{on the nonzero singular subspace}.
\end{aligned}
    \label{eq:appendix-polar-map}
\end{equation}
The polar map preserves the row and column singular vectors, replaces every
nonzero singular value by one, and retains the nonzero singular subspace of
$H_{l,\sigma,t}$.  The polar factor solves
\begin{equation}
\begin{aligned}
    \mathcal{P}(H)
    &\in
    \argmax_{\lVert D\rVert_2\le 1}\langle D,H\rangle_F,
    &
    \langle\mathcal{P}(H),H\rangle_F
    &=\lVert H\rVert_*,
    \\
    \mathcal{P}(aH)=\mathcal{P}(H)
    \quad(a>0,\ H\ne0).
\end{aligned}
    \label{eq:appendix-polar-steepest}
\end{equation}
The implementation uses
$D^\mu_{l,\sigma,t}=\operatorname{NS}_5(H_{l,\sigma,t})$, a finite
Newton--Schulz approximation to the polar direction, together with match-RMS
learning-rate adjustment \citep{liu2025muon}.

The branch equation in \eqref{eq:matrix-branch-step} is sequential because AdamW
and Muon keep separate states.  Let
$\mathcal D^{\rm Adam}_{l,\sigma,t}$ and
$\mathcal D^{\mu}_{l,\sigma,t}$ denote their complete first-order directions,
including bias correction, decoupled weight decay, and, for Muon, the finite
Newton--Schulz and match-RMS adjustments.  The sequential step has expansion
\begin{equation}
    \Delta M_{l,\sigma}
    =
    -\eta_t s^{\rm Adam}_{l,\sigma}(1-\mu_{l,\sigma,t})
    \mathcal D^{\rm Adam}_{l,\sigma,t}
    -\eta_t s_\mu\mu_{l,\sigma,t}
    \mathcal D^\mu_{l,\sigma,t}
    +O(\eta_t^2),
    \label{eq:appendix-branch-first-order}
\end{equation}
after the group control has set the role-specific gradient scales.  Flattening
the nonzero singular values gives weaker represented modes more relative
movement within the existing singular subspace.  MatrixPolicy confines this intervention
to the early window in \eqref{eq:matrix-branch-schedule}; AdamW and reciprocal
pair balancing remain after the Muon fraction reaches zero.

The Muon gate in \eqref{eq:matrix-branch-schedule} is monotone in the two layer
summaries.  Away from clipping boundaries and the ReLU kink, the derivatives of
$\log\psi_{l,t}$ with respect to $\Psi_{l,t}$ and $\Omega_{l,t}$ are $-0.30$
and $-0.65$ inside the $\psi$ clipping interval.  Thus the Muon fraction is
largest when role pressure and rational-coefficient activity are both small.
Large $|\pi_{l,g}|$ marks
unequal input/output pressure, and large $\alpha_{l,g}$ marks rational
coefficients moving strongly relative to the matrix pair.  These are precisely
the regimes where the spectrum-normalized substep is attenuated.

\subsection{Reciprocal Pair Balancing}
\label{app:matrixpolicy-pair-rescale}

After the descent branch, MatrixPolicy applies the reciprocal
reparameterization
\begin{equation}
    A_{l,g}^{+}=e^{\ell_{l,g,t}}A_{l,g},
    \qquad
    B_{l,g}^{+}=e^{-\ell_{l,g,t}}B_{l,g},
    \label{eq:appendix-reciprocal-move}
\end{equation}
every five optimizer steps.  The construction follows the positive coordinate in
Appendix~\ref{app:rlb-positive-coordinate}.  With the rational response held
fixed, the unfloored map satisfies
$h_{l,g}(\lambda A_{l,g}x)=\lambda h_{l,g}(A_{l,g}x)$ for $\lambda>0$, so
replacing $(A_{l,g},B_{l,g})$ by
$(\lambda A_{l,g},\lambda^{-1}B_{l,g})$ leaves $B_{l,g}h_{l,g}$ unchanged.
The floor-error bound in \eqref{eq:appendix-floor-bound} quantifies the
departure from this identity.

Equivalent unfloored representatives have different relative gradient
pressures.  Let
\begin{equation}
\begin{aligned}
    p^{A}_{l,g}
    &=
    \frac{\rms(\nabla A_{l,g})}{\rms(A_{l,g})},
    &
    p^{B}_{l,g}
    &=
    \frac{\rms(\nabla B_{l,g})}{\rms(B_{l,g})}.
\end{aligned}
\end{equation}
In the fixed-response, unfloored map,
$J_h(e^\ell z)=J_h(z)$ almost everywhere.  Evaluating the instantaneous
gradients at the transformed representative gives
\begin{equation}
\begin{aligned}
    \nabla_{A^+_{l,g}}\mathcal L
    &=e^{-\ell_{l,g,t}}\nabla_{A_{l,g}}\mathcal L,
    &
    \nabla_{B^+_{l,g}}\mathcal L
    &=e^{\ell_{l,g,t}}\nabla_{B_{l,g}}\mathcal L,
    \\
    p^{A,+}_{l,g}
    &=
    e^{-2\ell_{l,g,t}}p^{A}_{l,g},
    &
    p^{B,+}_{l,g}
    &=
    e^{2\ell_{l,g,t}}p^{B}_{l,g}.
\end{aligned}
    \label{eq:appendix-pressure-gauge}
\end{equation}
The transformation law concerns freshly evaluated pressures; the EMA memories
retain their temporal role in policy control.  Positive $\ell$ reduces
input-side relative pressure and increases output-side relative pressure.  The
$+0.25\bar\pi_{l,g}$ term in
\eqref{eq:rescale-signals} therefore raises $\tau_{l,g}$ when the input role
has larger recent pressure, selecting the corrective sign in
\eqref{eq:appendix-pressure-gauge}.

The curve-shape target follows from a separable role-pressure surrogate.  After
holding data- and orientation-dependent factors fixed within one group, write
\begin{equation}
    \widetilde p^A_{l,g}(\gamma)
    =
    \hat d^{\rm probe}_{l,g}e^{-\gamma},
    \qquad
    \widetilde p^B_{l,g}(\gamma)
    =
    \hat o^{\rm probe}_{l,g}e^{\gamma}.
\end{equation}
Their unique equality point is
\begin{equation}
    \gamma^\star_{l,g}
    =
    \frac{1}{2}
    \left(
    \log\hat d^{\rm probe}_{l,g}
    -
    \log\hat o^{\rm probe}_{l,g}
    \right),
    \label{eq:appendix-gamma-star}
\end{equation}
which supplies the first term of $\tau_{l,g}$.  This equality is a balance
coordinate for the scalar proxy.  Full data-dependent matrix gradients retain
the orientation and data factors held fixed by the surrogate.

For nonzero matrices and unfloored RMS values,
\begin{equation}
\begin{aligned}
    \gamma_{l,g}
    &=
    \log\rms(A_{l,g})-\log\rms(B_{l,g}),
    \\
    \gamma_{l,g}^{+}
    &=
    \gamma_{l,g}+2\ell_{l,g,t}.
\end{aligned}
    \label{eq:appendix-gamma-shift}
\end{equation}
Substituting the unclipped log step from \eqref{eq:rescale-step} gives
\begin{equation}
\begin{aligned}
    \zeta_{l,g,t}
    &=
    s_{l,t}\chi^{\rm rs}_{l,g},
    \\
    \gamma_{l,g}^{+}
    &=
    (1-\zeta_{l,g,t})\gamma_{l,g}
    +\zeta_{l,g,t}\tau_{l,g}.
\end{aligned}
    \label{eq:appendix-rescale-contraction}
\end{equation}
Since $\delta_l\in[0,1]$, the evaluated depth factor lies in
$[0.925,1.075]$.  Hence $0\le s_{l,t}\le0.5375$ and
$\chi^{\rm rs}_{l,g}\le e^{0.125}$, so
$0\le\zeta_{l,g,t}<0.61$.  The idealized unclipped coordinate is therefore a
strict contraction toward $\tau_{l,g}$ whenever $\zeta_{l,g,t}>0$.  The
$0.030$ log-step clip bounds its magnitude and shortens moves that exceed the
bound.  With floored RMS values,
$\gamma^+=\gamma+2\ell$ is approximate, and the implementation relies on the
local bound in Appendix~\ref{app:rlb-positive-coordinate}.

The activity factor has the opposite sign in this speed control.  Large
$\alpha_{l,g}$ damps gradient-driven matrix motion in \eqref{eq:group-policy}
and \eqref{eq:matrix-branch-schedule}; here
$\chi^{\rm rs}_{l,g}=\exp(0.10\bar\alpha_{l,g})$ controls the speed of the
post-step pair move, while the group and Muon controls determine
gradient-driven matrix motion.

\end{document}
