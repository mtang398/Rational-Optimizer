% Appendix-only theoretical note for an ICLR-style paper.
% Intended use: place after \appendix and include with \input{...}.

\providecommand{\rms}{\operatorname{rms}}
\providecommand{\MP}{\operatorname{MP}}
\providecommand{\AW}{\operatorname{AdamW}}
\providecommand{\R}{\mathbb{R}}
\providecommand{\cG}{\mathcal{G}}
\providecommand{\cQ}{\mathcal{Q}}
\providecommand{\cP}{\mathcal{P}}
\providecommand{\norm}[1]{\left\lVert #1 \right\rVert}
\providecommand{\ip}[2]{\left\langle #1,#2 \right\rangle}
\providecommand{\lammax}{\lambda_{\max}}
\providecommand{\lammin}{\lambda_{\min}}

\section{A Conditional Quotient-Optimization Separation}
\label{app:complete-opt-proof}
\label{app:gauge-conditioning}

\subsection{Quotient Geometry}
\label{app:complete-opt-proof:geometry}

For one RLB layer, suppress the layer index and write
\begin{align}
    z_g &= A_g x, \\
    r_g &= \sqrt{m^{-1}\norm{z_g}_2^2+\epsilon_{\rm rms}}, \\
    u_g &= z_g/r_g, \\
    h_g &= r_g R_g(u_g), \\
    y &= B\operatorname{concat}_{g=1}^G h_g .
\end{align}
For $a_g>0$, define
\begin{equation}
    T_a(A_g,B_g)=(a_gA_g,a_g^{-1}B_g),
    \qquad g=1,\ldots,G .
    \label{eq:complete-positive-action}
\end{equation}
Equivalently, with $s_g=\log a_g$,
\begin{equation}
    T_s(A_g,B_g)=(e^{s_g}A_g,e^{-s_g}B_g).
    \label{eq:complete-log-action}
\end{equation}
Let $\cP$ be the local RLB-matrix parameter space, let $\cG=(\R,+)^G$, and let
$\cQ=\cP/\cG$.

\begin{lemma}[Exact positive gauge]
\label{lem:exact-positive-gauge}
If $\epsilon_{\rm rms}=0$ and $r_g>0$ for every group on the considered input, then
$T_s(A,B)$ and $(A,B)$ represent the same layer map for all $s\in\R^G$.
\end{lemma}

\begin{proof}
For one group, $A_g'=e^{s_g}A_g$ gives $z_g'=e^{s_g}z_g$.  Since
$\epsilon_{\rm rms}=0$, $r_g'=e^{s_g}r_g$, hence $u_g'=u_g$ and
$h_g'=e^{s_g}h_g$.  The corresponding output block satisfies
$B_g'h_g'=e^{-s_g}B_g e^{s_g}h_g=B_gh_g$.  Summing over groups gives the claim.
\end{proof}

\begin{assumption}[Local quotient chart]
\label{ass:quotient-chart}
In a neighborhood of the trajectory, parameters can be written as
\begin{equation}
    \theta=\theta(q,s),
    \qquad q\in\R^d,
    \qquad s\in\R^G,
\end{equation}
where $q$ is a local coordinate on $\cQ$ and $s$ is the log-gauge coordinate.  The objective
satisfies
\begin{equation}
    L(\theta(q,s))=F(q).
    \label{eq:loss-quotient}
\end{equation}
For the exact statements, $\epsilon_{\rm rms}=0$.  Nonzero RMS stabilization is included
only through the residual model of Assumption~\ref{ass:uniform-perturbation}.
\end{assumption}

\begin{assumption}[Local quadratic model]
\label{ass:quadratic-objective}
The quotient objective is
\begin{equation}
    F(q)=F_\star+\frac{1}{2}(q-q_\star)^\top H(q-q_\star),
    \label{eq:quadratic-F}
\end{equation}
where $H\succ0$ and $0<\mu I\preceq H\preceq L I<\infty$.
\end{assumption}

\begin{lemma}[Gauge tangent orthogonality]
\label{lem:gauge-null-direction}
Under Lemma~\ref{lem:exact-positive-gauge}, for any differentiable loss depending on the
RLB matrices only through the represented function, with Frobenius inner products,
\begin{equation}
    \ip{\nabla_{A_g}L}{A_g}-\ip{\nabla_{B_g}L}{B_g}=0 .
    \label{eq:gauge-null-gradient}
\end{equation}
\end{lemma}

\begin{proof}
Differentiate $L(e^{s_g}A_g,e^{-s_g}B_g)=L(A_g,B_g)$ at $s_g=0$.
\end{proof}

\subsection{Canonicalized Quotient Preconditioning}
\label{app:complete-opt-proof:preconditioning}

For group $g$, define
\begin{equation}
    \gamma_g=\log \rms(A_g)-\log \rms(B_g).
    \label{eq:complete-gamma}
\end{equation}
The logarithms in this subsection are taken on true positive RMS quantities.
Stabilized RMS floors preserve the lemma exactly only when inactive; otherwise the
resulting discrepancy is part of the perturbation model in
Assumption~\ref{ass:uniform-perturbation}.
Given a target log-ratio $\tau_g$, the unclipped rebalance is
\begin{equation}
    \ell_g=\frac{1}{2}(\tau_g-\gamma_g),
    \qquad
    A_g^+=e^{\ell_g}A_g,
    \qquad
    B_g^+=e^{-\ell_g}B_g.
    \label{eq:complete-rebalance}
\end{equation}

\begin{lemma}[RMS-ratio canonicalization]
\label{lem:complete-targeting}
For true positive RMS values, the update in \eqref{eq:complete-rebalance} satisfies
\begin{equation}
    \log\rms(A_g^+)-\log\rms(B_g^+)=\tau_g .
\end{equation}
\end{lemma}

\begin{proof}
By homogeneity of RMS under positive scaling,
\begin{align}
    \log\rms(A_g^+)-\log\rms(B_g^+)
    &=\log\rms(e^{\ell_g}A_g)-\log\rms(e^{-\ell_g}B_g) \\
    &=\gamma_g+2\ell_g
      =\tau_g .
\end{align}
\end{proof}

\begin{lemma}[Gauge-dependent raw quotient step]
\label{lem:gauge-dependent-step}
Let $w>0$, $a=e^s\sqrt{w}$, $b=e^{-s}\sqrt{w}$, and
$\varphi(w)=\frac{1}{2}(w-y)^2$.  One Euclidean gradient-descent step in $(a,b)$ with step
size $\eta$ gives
\begin{equation}
    w^+=w-2\eta w\cosh(2s)(w-y)+O(\eta^2).
    \label{eq:scalar-gauge-step}
\end{equation}
\end{lemma}

\begin{proof}
Since $\partial\varphi/\partial a=(w-y)b$ and $\partial\varphi/\partial b=(w-y)a$,
\begin{align}
    w^+
    &=(a-\eta(w-y)b)(b-\eta(w-y)a) \\
    &=w-\eta(w-y)(a^2+b^2)+O(\eta^2).
\end{align}
Using $a^2+b^2=w(e^{2s}+e^{-2s})=2w\cosh(2s)$ gives
\eqref{eq:scalar-gauge-step}.
\end{proof}

\begin{assumption}[Frozen quotient preconditioners]
\label{ass:frozen-preconditioner}
During the local phase, MatrixPolicy and AdamW induce fixed positive definite quotient
preconditioners $P_{\MP}$ and $P_{\AW}$ satisfying
\begin{align}
    q_{t+1}^{\MP}
    &=q_t^{\MP}-\eta_{\MP}P_{\MP}\nabla F(q_t^{\MP}),
    \label{eq:mp-ideal-dynamics}\\
    q_{t+1}^{\AW}
    &=q_t^{\AW}-\eta_{\AW}P_{\AW}\nabla F(q_t^{\AW}).
    \label{eq:aw-ideal-dynamics}
\end{align}
Here $P_{\MP}$ is the quotient preconditioner after gauge rebalance and $P_{\AW}$ is the
quotient preconditioner induced by AdamW's raw-coordinate adaptive state.
\end{assumption}

For $P\succ0$, define
\begin{equation}
    K(P)=P^{1/2}HP^{1/2},
    \qquad
    \kappa(P;H)=\frac{\lammax(K(P))}{\lammin(K(P))}.
    \label{eq:precond-condition-number}
\end{equation}
Let
\begin{equation}
    \eta^\star(P)=\frac{2}{\lammax(K(P))+\lammin(K(P))},
    \qquad
    \rho(P;H)=\frac{\kappa(P;H)-1}{\kappa(P;H)+1}.
    \label{eq:rho-def}
\end{equation}

\begin{assumption}[Strict quotient-conditioning advantage]
\label{ass:conditioning-advantage}
\begin{equation}
    \kappa_{\MP}:=\kappa(P_{\MP};H)
    <
    \kappa(P_{\AW};H)=:\kappa_{\AW} .
    \label{eq:kappa-gap}
\end{equation}
\end{assumption}

\subsection{Contraction and Perturbation Bounds}
\label{app:complete-opt-proof:contraction}

\begin{lemma}[Preconditioned quadratic contraction]
\label{lem:preconditioned-contraction}
Let $F$ satisfy Assumption~\ref{ass:quadratic-objective}.  For $P\succ0$, consider
$q_{t+1}=q_t-\eta^\star(P)P\nabla F(q_t)$.  Then
\begin{equation}
    F(q_T)-F_\star
    \leq
    \rho(P;H)^{2T}\bigl(F(q_0)-F_\star\bigr),
    \label{eq:upper-contraction}
\end{equation}
and the bound is sharp.
\end{lemma}

\begin{proof}
Let $e_t=q_t-q_\star$ and $\widetilde e_t=P^{-1/2}e_t$.  Then
\begin{equation}
    \widetilde e_{t+1}
    =\left(I-\eta^\star(P)K(P)\right)\widetilde e_t,
    \qquad
    2(F(q_t)-F_\star)=\widetilde e_t^TK(P)\widetilde e_t.
\end{equation}
Diagonalize $K(P)=U\Lambda U^T$ and set $v_t=U^T\widetilde e_t$.  The largest multiplier
satisfies
\begin{equation}
    \max_i\left|1-\eta^\star(P)\lambda_i\right|
    =
    \frac{\lammax(K(P))-\lammin(K(P))}
    {\lammax(K(P))+\lammin(K(P))}
    =\rho(P;H).
\end{equation}
Thus
\begin{align}
    2(F(q_{t+1})-F_\star)
    &=\sum_i \lambda_i v_{t+1,i}^2 \\
    &\leq \rho(P;H)^2\sum_i \lambda_i v_{t,i}^2 \\
    &=2\rho(P;H)^2(F(q_t)-F_\star).
\end{align}
Iteration gives \eqref{eq:upper-contraction}.  Equality holds when the transformed
initial error $\widetilde e_0=P^{-1/2}(q_0-q_\star)$ is an eigenvector of $K(P)$
associated with $\lammin(K(P))$ or $\lammax(K(P))$.
\end{proof}

\begin{theorem}[Conditional worst-case optimization separation]
\label{thm:conditional-optimization-separation}
Suppose Assumptions~\ref{ass:quotient-chart}--\ref{ass:conditioning-advantage} hold, and
set $\eta_{\MP}=\eta^\star(P_{\MP})$ and $\eta_{\AW}=\eta^\star(P_{\AW})$.  Then the
worst-case contraction factors satisfy, for every integer $T\geq1$,
\begin{equation}
    \sup_{q_0\neq q_\star}
    \frac{F(q_T^{\MP})-F_\star}{F(q_0)-F_\star}
    =
    \left(\frac{\kappa_{\MP}-1}{\kappa_{\MP}+1}\right)^{2T}
    <
    \left(\frac{\kappa_{\AW}-1}{\kappa_{\AW}+1}\right)^{2T}
    =
    \sup_{q_0\neq q_\star}
    \frac{F(q_T^{\AW})-F_\star}{F(q_0)-F_\star}.
    \label{eq:main-separation}
\end{equation}
\end{theorem}

\begin{proof}
The map $\kappa\mapsto(\kappa-1)/(\kappa+1)$ is strictly increasing for
$\kappa>0$.  Assumption~\ref{ass:conditioning-advantage} gives
$\rho(P_{\MP};H)<\rho(P_{\AW};H)$.  Lemma~\ref{lem:preconditioned-contraction} applied to
$P_{\MP}$ and $P_{\AW}$ gives the displayed factors, and sharpness gives both
equalities.
\end{proof}

\begin{assumption}[Uniform local perturbation bound]
\label{ass:uniform-perturbation}
For a fixed horizon $T$, the implemented quotient iterates satisfy
\begin{align}
    q_{t+1}^{\MP,{\rm impl}}
    &=q_t^{\MP,{\rm impl}}-\eta_{\MP}P_{\MP}\nabla F(q_t^{\MP,{\rm impl}})+r_t^{\MP}, \\
    q_{t+1}^{\AW,{\rm impl}}
    &=q_t^{\AW,{\rm impl}}-\eta_{\AW}P_{\AW}\nabla F(q_t^{\AW,{\rm impl}})+r_t^{\AW},
\end{align}
with $\norm{r_t^{\MP}}_2+\norm{r_t^{\AW}}_2\leq\delta$ for all $0\leq t<T$.
The residuals include the RMS floor, denominator floors, clipping, moment lag, weight
decay, and nonquadratic Taylor remainders.
\end{assumption}

\begin{proposition}[Stability of a strict finite-time gap]
\label{prop:complete-perturbation}
Fix $T\geq1$ and suppose the idealized dynamics satisfy
\begin{equation}
    m_T:=F(q_T^{\AW})-F(q_T^{\MP})>0 .
    \label{eq:finite-gap}
\end{equation}
Assume the implemented and ideal recursions use the same initial points, and assume the
ideal trajectories lie in a compact set $K\subset\operatorname{int}(U)$, where $U$ is a
compact neighborhood on which $F$ and the ideal one-step maps are Lipschitz.  Then
there exists $\delta_0>0$ such
that, whenever Assumption~\ref{ass:uniform-perturbation} holds with
$\delta<\delta_0$, the implemented trajectories remain in $U$ through time $T$ and
\begin{equation}
    F(q_T^{\AW,{\rm impl}})-F(q_T^{\MP,{\rm impl}})>\frac{m_T}{2}>0 .
    \label{eq:stable-gap}
\end{equation}
\end{proposition}

\begin{proof}
Let $\Psi_{\MP}$ and $\Psi_{\AW}$ be the ideal one-step maps, and let $C_\Psi$ be a
common Lipschitz constant on $U$.  Let $r_{\rm stay}=\operatorname{dist}(K,U^c)>0$.
By induction, as long as the implemented
trajectories remain in $U$,
\begin{equation}
    \norm{q_t^{\MP,{\rm impl}}-q_t^{\MP}}
    +
    \norm{q_t^{\AW,{\rm impl}}-q_t^{\AW}}
    \leq C_T\delta
\end{equation}
for a constant $C_T$ depending only on $T$ and $C_\Psi$.  Taking
$\delta\leq r_{\rm stay}/(2C_T)$ keeps the implemented trajectories in $U$ and
closes the induction.  If $C_F$ is a Lipschitz constant for $F$ on $U$, then
\begin{equation}
    \left|F(q_T^{\MP,{\rm impl}})-F(q_T^{\MP})\right|
    +
    \left|F(q_T^{\AW,{\rm impl}})-F(q_T^{\AW})\right|
    \leq C_F C_T\delta .
\end{equation}
Taking
\begin{equation}
    \delta_0=\min\left\{\frac{m_T}{2C_FC_T},\frac{r_{\rm stay}}{2C_T}\right\}
\end{equation}
gives the claim.
\end{proof}
