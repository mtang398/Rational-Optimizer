\documentclass{article}

\usepackage{iclr2027_conference,times}
\input{math_commands.tex}

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{mathtools}
\usepackage{microtype}
\usepackage{url}
\usepackage{booktabs}
\usepackage{array}
\usepackage{tabularx}
\usepackage{flafter}

\newtheorem{proposition}{Proposition}
\newtheorem{lemma}{Lemma}
\newcommand{\grain}{\textsc{Grain}}
\newcommand{\tiller}{\textsc{Tiller}}
\newcommand{\frob}[1]{\left\lVert #1\right\rVert_{\mathrm F}}
\newcommand{\inner}[2]{\left\langle #1,#2\right\rangle}

\title{Loss-Informed Transformer Training\\
with Learned Rational Activations}

\author{Anonymous authors}

\begin{document}

\maketitle

\begin{abstract}
We introduce a joint activation--optimizer design that achieves lower mean
validation loss than AdamW, Muon, and SOAP in Transformer pretraining at
matched training-token budgets. Our approach uses the model's learned
nonlinear responses to guide both the construction of matrix updates and
their joint allocation across groups and layers. \textbf{GRAIN},
\textbf{G}roupwise \textbf{R}ational \textbf{A}ctivation with \textbf{I}nternal
\textbf{N}ormalization, learns a rational activation function for each
feature group in normalized coordinates, allowing different groups to
adapt their nonlinear response shapes during training.
\textbf{TILLER}, \textbf{T}angent-\textbf{I}nformed \textbf{L}oss
\textbf{L}edger for \textbf{E}qual-budget \textbf{R}eweighting, uses these
learned responses to shape matrix updates and estimate their first-order effects on
the loss. Measuring these effects jointly reveals how the loss-response patterns of
different updates reinforce or offset one another, information that their
individual average descent scores do not capture. TILLER maintains a
running summary of these cross-update relationships and combines it with
current gradients to determine the relative strengths of updates across
groups and layers.
The gains hold across every tested corpus and model size with both SwiGLU
and GRAIN baseline models, demonstrating improvements beyond activation
replacement alone.
\end{abstract}

\section{Introduction}

Activation design and optimization offer two routes to improving
Transformer pretraining at a fixed training-token budget. Nonlinearities
such as GELU and SwiGLU improve the transformations available to a model
\citep{hendrycks2016gelu,shazeer2020glu}, while matrix-aware optimizers
such as SOAP and Muon improve the updates used to learn them
\citep{vyas2025soap,liu2025muon}. These choices are connected by the
activation function: its shape determines both the features a layer
produces and how changes to the surrounding weights propagate through
the network. Learning that shape therefore creates an opportunity to
improve the representation and guide its optimization together.

Trainable rational activations provide a concrete basis for this approach.
A small set of coefficients specifies a flexible nonlinear curve that can
be learned jointly with the network weights \citep{molina2020pade}.
Their expressive power is supported by approximation-theoretic
separations from ReLU and bounded-parameter smooth-activation networks
\citep{boulle2020rational,tang2026expressivity}, and group sharing brings
them into Transformer feed-forward blocks \citep{yang2025kat}.
The learned features and their derivatives describe how a proposed
matrix update changes a feed-forward block. Combining this change with
the downstream loss derivative gives its first-order loss response.
This response provides a common basis for comparing updates to groups
whose activation shapes and sensitivities evolve differently during
training.

Individual descent scores leave an important allocation decision
unresolved. A score summarizes a direction's first-order effect on the
average loss, but averages discard how its responses vary across
example--token positions. Directions with equally favorable scores can
reinforce one another at the same positions or have responses that
partly offset. Their average descent can be the same even when the
magnitudes of their combined responses differ. Measuring the directions
at shared positions preserves these relationships across groups and
layers. An optimizer can then balance current descent against the
magnitude of the joint response when choosing their relative strengths.

We introduce a joint activation--optimizer design that uses this
information to construct a training step. GRAIN, Groupwise Rational
Activation with Internal Normalization, learns a separate rational curve
for each feature group in normalized coordinates and restores the input
scale at its output. TILLER, Tangent-Informed Loss Ledger for Equal-budget
Reweighting, uses the learned features and their derivatives to shape
matrix update directions. It maintains a compact running summary of
the joint loss responses of paired incoming and outgoing feed-forward
directions and combines this history with current gradients to allocate update strength across
groups and layers. The allocation preserves the combined norm of the
feed-forward directions before weight decay while redistributing their
relative strengths according to the response model.

We develop full-horizon and staged training from the same TILLER update.
Full-horizon training continuously adapts directions and their allocation
as the representation evolves. Staged training limits response
collection and joint allocation to an initial interval, then continues
from the learned model with a chosen optimizer. After the transition,
matrix updates use the rule and computational cost of that optimizer.
GRAIN remains trainable throughout both protocols.

We evaluate Transformer pretraining on five corpora at two model sizes,
with three seeds and 100M training tokens per run. GRAIN with
full-horizon TILLER achieves lower mean validation loss than AdamW,
Muon, SOAP, CAME \citep{luo2023came}, and Schedule-Free AdamW
\citep{defazio2024schedulefree} with either SwiGLU or GRAIN
on every corpus at both sizes. At the larger model size, the evaluated
staged configuration also achieves lower mean validation loss than all
five baseline optimizers under
both activations and takes less total training time than full-horizon
TILLER on every corpus. The gains over GRAIN baselines show that the
improvements extend beyond activation replacement alone.

[TODO: Experiment section]

\section{Related Work}

Learned activations expand the nonlinear transformations available within
a fixed network architecture. PReLU learns the negative slope of a
rectifier \citep{he2015rectifiers}, while Pad\'{e} Activation Units learn
the numerator and denominator of a rational curve \citep{molina2020pade}.
Kolmogorov--Arnold Transformer brings this flexibility to Transformer
blocks through group-shared rational functions; its reported
configuration shares numerator coefficients across groups
\citep{yang2025kat}. GRAIN learns both numerator and denominator
coefficients independently for each group.

Approximation theory provides a complementary motivation for rational
activations. Rational networks admit parameter-efficiency separations
from ReLU networks \citep{boulle2020rational}, with subsequent results
extending the comparison to bounded-parameter smooth-activation networks
and to gated and Transformer-style constructions. The accompanying
normalization analysis identifies redundant degrees of freedom between
rational coefficients and learned affine transforms
\citep{tang2026expressivity}. Layer Normalization and
RMSNorm control feature scale \citep{ba2016layer,zhang2019rmsnorm}.
GRAIN uses the group RMS internally and restores it after the rational
transform, without additional learned affine parameters. The nonlinear
curve thus acts on relative feature magnitudes while its output retains
input-amplitude information.

Structured optimizers exploit the organization of neural-network
parameters to construct update directions. Adafactor estimates second
moments from row and column statistics \citep{shazeer2018adafactor}, and
CAME refines factored adaptation using confidence in the update
\citep{luo2023came}. Shampoo uses matrix-valued preconditioners
\citep{gupta2018shampoo}; SOAP combines this structure with Adam updates
in the preconditioner's eigenbasis \citep{vyas2025soap}. Muon instead
orthogonalizes matrix momentum \citep{jordan2024muon,liu2025muon}, a
construction related to steepest descent under matrix norms
\citep{bernstein2024old}. TILLER builds on orthogonalized and factorized
adaptive directions, using learned activation responses to guide a
norm-preserving rotation.

Functional sensitivity also informs optimization through curvature and
restricted search spaces. K-FAC approximates Fisher blocks using moments
of activations and backpropagated derivatives, including cross-layer
blocks \citep{martens2015kfac}. Newton--Muon incorporates input
second moments through a quadratic model of a matrix perturbation
\citep{du2026newtonmuon}. Krylov Subspace Descent optimizes the training
objective within a subspace generated using gradient and curvature
information \citep{vinyals2012krylov}. TILLER places its joint model on
coefficients of paired feed-forward directions. Products of their
first-order loss responses at shared positions supply a quadratic
penalty on simultaneous updates.

Optimizer switching combines update rules at different stages of
training. SWATS uses a projection-based criterion to transition from
Adam to SGD \citep{keskar2017switching}. Staged TILLER treats the duration
of response-based coordination and the choice of continuation optimizer
as separate training-protocol choices.



\section{Method}
\label{sec:method}

We introduce GRAIN in Transformer feed-forward blocks and TILLER
to coordinate matrix updates using GRAIN's loss response. We first define
the block, then the update shared by full-horizon and staged training.

\subsection{GRAIN}

GRAIN (Groupwise Rational Activation with Internal Normalization)
replaces a fixed nonlinearity with group-shared rational functions.
For input $x\in\mathbb R^d$, partition the intermediate features
into $G$ groups of width $w$. The incoming and outgoing projections
of group $g$ are $A_g\in\mathbb R^{w\times d}$ and
$B_g\in\mathbb R^{d\times w}$. Its feed-forward block is
\begin{equation}
 \begin{gathered}
 z_g=A_gx,\qquad
 \rho_g=\sqrt{\|z_g\|_2^2/w+\epsilon_{\rm act}},\\
 h_g=\rho_g\phi_g(z_g/\rho_g),\qquad
 y=\sum_{g=1}^{G}B_gh_g.
 \end{gathered}
 \label{eq:overview-grain}
\end{equation}
The curve acts coordinatewise on RMS-normalized features and its
output is multiplied by the same group scale
\citep{zhang2019rmsnorm}. The scale is computed per token and
group, with stabilizer $\epsilon_{\rm act}>0$.

Each group learns a rational curve \citep{molina2020pade,yang2025kat}:
\begin{equation}
 \phi_g(u)=\frac{\sum_{j=0}^{5}a_{gj}u^j}
 {1+\sum_{j=1}^{4}|b_{gj}|\,|u|^j}.
 \label{eq:overview-rational}
\end{equation}
The denominator is at least one, so the curve has no poles.
All coefficients are shared across channels and token positions
within a group and learned independently across groups. We initialize
each curve with the same rational approximation to SiLU and train
its coefficients jointly with the projection weights.
Appendix~\ref{sec:grain} derives the Jacobian of the normalized
activation and its dependence on input scale.

\subsection{TILLER}

TILLER (Tangent-Informed Loss Ledger for Equal-budget Reweighting)
uses the learned activation response to construct matrix directions and
allocate their strengths jointly. For an $L$-layer model, let
$i=(\ell,g)$ index the $C=LG$ feed-forward groups. Each group receives
incoming and outgoing directions $(q_i^A,q_i^B)$ and a shared coefficient
$c_i$. The directions determine how each group changes; the coefficients
coordinate these changes across the model.

For either matrix in a pair, a calibrated polar direction $p$ and a
factorized adaptive direction $a$ transform the same momentum
\citep{jordan2024muon,liu2025muon,shazeer2018adafactor}. Write
$a^\perp=a-\inner{a}{p}p/\frob p^2$.

The incoming direction is guided by the activation Jacobian,
and the outgoing direction by the activated features.
For each role, loss-weighted participation $\pi\in[0,1]$ measures
the spread of this response. Congruence $\kappa\in[0,1]$ compares
its response kernel with that of the initial rational curve
at the same current inputs. These summaries set the rotation:
\begin{equation}
 \widetilde q=\sqrt{1-e}\,p+
 \sigma\sqrt e\frac{\frob p}{\frob{a^\perp}}a^\perp,
 \qquad e=\pi(1-\kappa^2),
 \label{eq:overview-rotation}
\end{equation}
where $\sigma$ makes the tangent's gradient contraction nonnegative.
The rotation preserves the parent norm.
An unchanged response kernel retains $p$; broader responses permit
larger rotations for a given departure from initialization.
We retain $p$ for a degenerate or nonfinite rotation, or when the
parent $p$ or candidate $\widetilde q$ has nonpositive gradient
contraction. Matrix-size scaling of the selected direction gives $q$.
Appendix~\ref{sec:directions} defines the summaries, selection
conditions, and norm-preserving geometry.

To coordinate these directions, periodic probes measure their loss
responses at $K$ example--token positions shared across layers.
The probes also refresh $\pi$ and $\kappa$; between probes, directions
are recomputed from current moments using the latest response summaries.
For block input
$x_{k\ell}$, activated features $h_{ki}$, and activation Jacobian
$J_{ki}=\partial h_{ki}/\partial z_{ki}$, the paired response is
\begin{equation}
 S_{t,ki}=\inner{\xi_{k\ell}}
 {B_iJ_{ki}q_i^Ax_{k\ell}+q_i^Bh_{ki}},
 \label{eq:overview-score}
\end{equation}
where $\xi_{k\ell}$ is the derivative of the summed minibatch loss at
the block output, multiplied by the gradient-clipping factor.
The combined response is $S_tc$: its squared norm includes cross
terms between different groups and layers.
Appendix~\ref{sec:loss-measurements} derives this differential and
its relation to current-gradient descent.

A response matrix contributes the Gram metric $S_t^\top S_t/K$,
whose off-diagonal entries measure interactions between pairs.
The response $a_t^{\rm dec}$ to feed-forward weight decay contributes
the cross term $S_t^\top a_t^{\rm dec}/K$.
Their discounted histories supply the current metric $G_t$ and
cross term $r_t$. Between probes, current gradient contractions
supply repeated rows calibrated to the latest measured response scale.
A Robust Frequent Directions sketch retains this history as a row
factor and an isotropic remainder
\citep{ghashami2016frequent,luo2019robust}.
The newest rows enter the metric before compression.
Appendix~\ref{sec:ledger} gives the ledger construction and its
approximation bounds.

Let $g_i^A,g_i^B$ be the accumulated, averaged, clipped gradients,
$s_{t,i}=\inner{g_i^A}{q_i^A}+\inner{g_i^B}{q_i^B}$ their descent
score, and $\omega_i=\frob{q_i^A}^2+\frob{q_i^B}^2$.
The allocation balances current descent against the joint loss response:
\begin{equation}
 \begin{gathered}
 F_t(c)=-\eta_ts_t^\top c+
 \frac{\eta_t^2}{2}(c^\top G_tc+2r_t^\top c),\\
 \sum_i\omega_ic_i^2=\sum_i\omega_i,
 \end{gathered}
 \label{eq:overview-coordinate-model}
\end{equation}
where $\eta_t$ is the learning rate.
The linear term rewards current descent; the quadratic term penalizes
the joint response of the directions and feed-forward weight decay.
The constraint preserves the combined feed-forward direction norm of
$c=\boldsymbol1$, before decay, while permitting signed coefficients.
A coefficient proposal is used when
it improves this model and satisfies the norm and layerwise
contraction conditions; otherwise $c=\boldsymbol1$.
Appendix~\ref{sec:transaction} gives the projected solver and acceptance
conditions.

With decay rate $\lambda_{\rm wd}$, the resulting step applies
\[
 A_i\leftarrow(1-\eta_t\lambda_{\rm wd})A_i-\eta_tc_iq_i^A,
 \qquad
 B_i\leftarrow(1-\eta_t\lambda_{\rm wd})B_i-\eta_tc_iq_i^B.
\]
The coefficients jointly allocate these feed-forward pairs.
An attention matrix $D$ uses a response-guided direction $q^D$ formed
from its own moments and layerwise GRAIN summaries, with update
$D\leftarrow(1-\eta_t\lambda_{\rm wd})D-\eta_tq^D$.
Rational coefficients and all remaining parameters use AdamW
\citep{loshchilov2019decoupled}.
Tables~\ref{tab:notation} and~\ref{tab:algorithm} collect the notation
and the complete training loop.

\subsubsection{Full-horizon TILLER}

For training horizon $T$, full-horizon TILLER applies this common
update at every step. It continually adapts the matrix directions
and their joint allocation as GRAIN's nonlinearities evolve.

\subsubsection{Staged training}

For a chosen transition $T_s<T$, staged training applies the same
TILLER update through $T_s$ and then continues the feed-forward and
attention updates with an optimizer $\mathcal U$.
Its state initializer $\mathcal I$ transfers stored first-moment
histories compatible with the continuation's recurrence and matrix
grouping; additional statistics use $\mathcal U$'s initialization.
The first continuation update acts on the learned matrices at $T_s+1$.
Model parameters and the learning-rate schedule continue across
the transition. Rational coefficients and remaining parameters retain
their AdamW state, and GRAIN remains trainable.
Response collection and ledger updates end at $T_s$.

\section{Experiments}

[TODO: Experiment section]

\section{Discussion}

\section{Conclusion}

\subsection*{AI use statement}

Generative AI tools assisted with method development, experimental planning,
implementation, mathematical derivations and proof checking, literature
analysis, result interpretation, and manuscript drafting and revision.

\bibliography{references}
\bibliographystyle{iclr2027_conference}

\clearpage
\appendix
\input{appendix}

\end{document}
